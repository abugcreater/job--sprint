# 开发与验证流程

1. 将回答面板拆出 `InterviewPage`，保持现有题目、评分和 Evidence Gate 文案。
2. 新增口述回答草稿保护 hook，统一清空、题型切换、题目切换和保存后的状态重置。
3. 将非空回答注册到 `useRouteLeaveGuard`，并把输入时的题目 ID 固定为本次草稿上下文。
4. 增加回归：离页后继续编辑、清空后继续编辑、切换题型后确认放弃，以及放弃后不创建证据。
5. 运行类型检查、React 测试、构建、Android 资源同步与 debug 编译、根功能流、架构和敏感扫描。
