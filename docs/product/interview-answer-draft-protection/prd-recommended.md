# 推荐方案

## 用户路径

1. 用户开始输入时，当前候选题成为本次回答的上下文。
2. 用户离开页面、清空回答、切换题型或选择另一题时，如果回答非空，先显示确认。
3. 选择“继续编辑”时，文本、题目和评分状态都保留。
4. 选择“放弃修改”时，才清空草稿并执行原来的清空或题目切换。
5. 保存时仍按原有路径写入当前任务的 Evidence Gate，随后关闭脏状态。

## 成功标准

- 非空回答不会在导航或明确替换操作中无提示消失。
- 用户不能把 A 题的未保存回答误记为 B 题证据。
- 空回答保持原来的快速筛选、切换和保存禁用体验。
