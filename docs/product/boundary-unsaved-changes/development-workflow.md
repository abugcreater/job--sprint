# 知识边界未保存修改保护开发与验证流程

日期：2026-07-31

1. 为边界草稿建立独立克隆和 12 字段 dirty 判断。
2. 将“请求替换”和“真正替换草稿”拆开；仅 dirty 时进入确认态。
3. 将 AI 候选修订的反馈写入与候选移除封装进确认后的动作。
4. 在知识边界面板渲染可访问确认框，保持现有按钮、焦点和视觉语义。
5. 用页面测试覆盖新建切换、已有编辑取消、继续/放弃、保存后的快速取消和候选修订延后写入。
6. 执行类型检查、前端回归、构建、根门禁、产品迭代校验和敏感扫描。
