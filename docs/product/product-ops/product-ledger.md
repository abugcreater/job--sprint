# 产品决策账本

日期：2026-07-16

## 当前主合同

| 项目 | 内容 |
|---|---|
| 产品包 | `docs/product/it-job-coach-v1/` |
| 推荐 PRD | `docs/product/it-job-coach-v1/prd-recommended.md` |
| 产品定位 | 面向泛 IT 求职者的 AI 求职教练与执行工作台。 |
| 阶段 | 准生产演示级 / 邀请制小范围产品化迭代中。 |
| 北极星指标 | 每周有效求职推进数。 |
| AI 契约 | AI 只生成草稿；用户接受后才写入日程、知识边界或候选题；拒绝必须记录原因。 |
| 当前实现 | React Web 与 Android 本地 WebView 已有画像、知识边界、自定义日程、AI 草稿、接受/拒绝和今日页联动；React 教练页新增邀请制首登编排，按账号/数据域、首登画像模板、批量素材与 3 条知识边界、首条个人日程、首条 AI 草稿确认展示 1/5 到 5/5 进度，并显示首登完成率、当前放弃点、风险等级和下一步动作；快速初始化支持 11 个角色族模板，并可追加 JD、简历、面试反馈和学习笔记素材段；新增 `/api/coach/onboarding-events`，React 可手动记录首登观察，Node fallback 按 `dataScope` 写入 `progress.coachOnboardingEvents`，Rust 正式服务写入独立 `coach_onboarding_events` 表，Node/Rust GET 均返回最新完成率、放弃点、最高风险、下一步和首登状态摘要；新增 `/api/coach/onboarding-report`，账号配置新增 `inviteBatch`，owner 可按邀请批次查看总人数、已开始、已完成、完成率、最高风险、Top 放弃点和用户最新首登状态，普通用户只看自己 `dataScope`，React 教练页已新增只读“邀请批次首登看板”；新增 `/api/coach/invitations`，owner 可登记邀请账号、数据域、批次、角色族、目标岗位、状态和备注，Node fallback 写入 owner `progress.coachInvitations`，Rust 正式服务写入独立 `coach_invitations` 表；当服务端配置仓库外 `JOB_SPRINT_USERS_FILE` 且未使用 `JOB_SPRINT_USERS_JSON` 时，React 邀请账号管理可为邀请用户开通或重置登录账号，禁用、恢复、删除单个或批次登录账号，并按邮件/IM/手工渠道生成可复制邀请通知草稿，Node/Rust 合同覆盖密码 hash 写入、新用户登录、旧密码失效、响应不含密码/hash、通知草稿不泄露密码/hash、runtime 隔离和 `accountAuditEvents` 账号生命周期审计读回；新增 `/api/coach/artifacts`，Node 与 Rust 均支持 Anthropic-compatible provider + fallback；新增 `/api/coach/boundary-suggestions`，用户粘贴 JD、简历片段或面试反馈后可生成候选知识边界，采纳后才写入正式边界，且正式边界保留来源摘要、置信度、provider、prompt version 和输入 hash，Node/Rust 均覆盖权限、schema、通用技术主题提取、Anthropic-compatible provider 成功、已有主题过滤和 provider timeout fallback；Rust `llm_runs` 已记录 provider、model、token、延迟和可选成本；新增 `/api/coach/feedback`，Node fallback 按 `dataScope` 写入 `progress.coachFeedback`，Rust 正式服务写入独立 `llm_feedback` 表，Node/Rust GET 均返回采纳率、拒绝类型、拒绝原因和下一轮提示校准 `summary`；新增 `/api/coach/outcomes`，Node/Rust 可按周从 runtime 计算有效推进、AI 采纳日程完成率、面试复盘率、延期和证据分布，并写入 `coachOutcomeSnapshots`；React 本地 AI 反馈复盘已能按 `acceptedFromArtifactId` 统计采纳 AI 日程草稿的今日任务完成率；React 复盘页已能生成本地规则版 AI 分析、本地 7 日周复盘归因，并接入服务端周结果归因读写入口；AI 草稿已接入 11 个泛 IT 角色族 playbook 和首批角色追问题卡，React/Node/Rust fallback 和 provider prompt 都会带角色视角、证据类型、回答框架、主候选题和追问库；投递/机会记录已被提取为机会/JD 信号并进入 React 本地生成、Node fallback、Rust fallback 和 provider prompt，React/Node/Rust fallback 会进一步生成规则版 `JD解析`，把岗位责任、硬技能、风险信号、证据要求和候选追问写入知识卡、日程建议、候选题、sources/reason/inputSummaryHash；Node `dataScope` 与 Rust/SQLite `scope` 已有 coach runtime 多用户隔离测试。 |
| 当前验证 | 本地 Web、React 单测、Android 本地 WebView、Rust/SQLite、本地 release gate、产品迭代工作流门禁、Node/Rust 多用户隔离、Node/Rust coach artifact API、Node/Rust coach boundary suggestions API、boundary suggestions provider/fallback 合同、React 采纳后边界 provenance、Node/Rust coach feedback API 与 feedback summary、Node/Rust coach outcomes API 与周结果快照、Node/Rust coach invitations API、Node/Rust 批量邀请导入、Node/Rust users file 账号开通/重置/禁用/恢复/删除合同、Node/Rust 批量登录账号禁用/恢复/删除合同、Node/Rust 账号生命周期审计事件、Node/Rust 邀请通知草稿合同、远端 users-file 账号生命周期、批量账号动作、`invitation_notifications_generated` smoke 和远端 UI 登录切换 smoke、React Coach 页 `本周有效推进`/`采纳后完成`/`面试复盘` 结果指标、React 本地采纳日程完成率归因、React 本地规则版复盘 AI 分析、React 本地 7 日周复盘归因、React Review 页服务端周结果归因入口、11 个角色族 playbook 与追问题卡单测、机会/JD 信号、`JD焦点` 与规则版 `JD解析` 生成测试、服务器同步、`job-sprint.service` 重启、服务器 HTTP 远端验收、远端 React 入口、远端邀请台账 smoke、远端 DeepSeek coach artifact/outcomes smoke、远端 `JD焦点` smoke、远端规则版 `JD解析` smoke 和正式 APK 验签已有通过记录。 |
| 交付边界 | 完整 HTTPS 生产交付仍以 `npm run validate:delivery` 为准；失败时不能宣称远端生产已完成。 |
| 团队状态 | 当前线程因继承智能体异常进入 `current_thread_quarantine=true`，本轮由主线程降级完成多角色审计，不能标 `TEAM_ROOM_PASS`。 |

## 决策记录

| 日期 | 决策 | 理由 | 取舍 |
|---|---|---|---|
| 2026-07-16 | Stats 与 AI 运行记录必须复用同一诊断规则。 | 只在 Coach 页解释 `local-fallback / server_unavailable` 会让用户在统计页仍把环境降级计入 provider 失败，导致跨模块指标和恢复动作互相矛盾。 | 抽出 `diagnoseLlmRun` 供 Coach 与 Stats 共同使用；Stats 只显示最近运行的诊断和建议动作，不主动探测、请求 API 或写入运行记录。 |
| 2026-07-16 | GitFlow 基线先比较文件树，再判断提交祖先。 | rebase、squash 或 merge commit 会让提交 SHA 与领先数量不同；只看历史会把内容已对齐的分支误判为阻塞，导致每日迭代停摆。 | `git diff --quiet origin/main origin/develop` 为 0 时标记“历史差异、内容已对齐”，从最新 `develop` 正常开发；只有文件树存在真实差异时才核对回同步 PR、required check 与人工合并状态。 |
| 2026-07-12 | GitFlow 远端保护升级为真实 PR 必过门禁。 | 仅有分支命名和本地脚本不能阻止 GitHub 上的错误目标 PR、直接推送或强推；必须先用真实 PR 证明 Actions 检查名称，再将其加入受保护分支的 required checks。 | `main/develop` ruleset `Protect main and develop` 已禁止删除、强推和绕过，要求 PR 并要求 GitHub Actions `validate`（integration `15368`）；PR #6 当时已实际运行并通过 `GitFlow Policy / validate`。后续分支对齐以文件树优先，历史差异不再单独阻塞普通需求。 |
| 2026-07-11 | 产品 UI 合并以新工作台结构为视觉基线，同时保留 main 的破坏性操作确认与短时撤销能力。 | 整页选择任一分支都会分别丢失产品化交互或用户数据恢复能力。 | 冲突按行为逐项裁决，并用页面测试、destructive-actions 测试和 Android remote 验收覆盖。 |
| 2026-07-11 | Android 文件导入、HTTPS 远端页面和离线 React 回退必须来自同一发布版本。 | 只更新服务端会让离线包与线上行为分叉，用户在网络回退时重新遇到旧交互。 | 每次正式发布先 `sync:android-react`，再验签 APK；真实地址和签名材料仍只允许仓库外注入。 |
| 2026-07-10 | AI 运行失败先做 runtime 分层诊断，再判断 provider。 | 用户看到 AI 运行记录长期失败时，真实问题可能是 5173 没有监听、纯 Vite 前端返回 HTML、API 路由缺失、鉴权缺失、服务端 5xx、provider 未配置、provider 调用失败 fallback 或 schema 失败；如果 UI/验收只看 `local-fallback / server_unavailable`，会把环境层问题误判成大模型质量问题。 | 新增 `tools/diagnose_coach_artifacts_runtime.js`、`npm run diagnose:coach-runtime` 和 `tests/coach_artifacts_runtime_diagnostic_test.js`，用非破坏性 POST smoke 分类 `runtime_unreachable`、`frontend_html_fallback`、`auth_required`、`api_unavailable`、`provider_not_configured`、`provider_failed_fallback`、`schema_failed` 和 `provider_success`；产品迭代门禁要求诊断脚本和测试入口持续存在。默认端口当前诊断为 `runtime_unreachable`，只说明本地 runtime 没启动，不替代远端 provider evidence 或真实 AI 质量归因。 |
| 2026-07-20 | Rust health 的 provider 状态必须和实际生成链路使用同一套“非空配置”口径。 | 空字符串环境变量常用于本地测试和显式关闭 provider；若 health 只判断环境变量是否存在，就会显示已配置，但 `/api/coach/artifacts` 实际回退本地规则，用户无法分辨环境问题与模型失败。 | `health_routes.rs` 只在 `ANTHROPIC_BASE_URL` 与 `ANTHROPIC_AUTH_TOKEN` 均为非空时标记 `apiConfigured=true`，空模型名返回空值；`test:rust-coach-runtime` 使用临时端口/SQLite/免登录进程，证明 health 为未配置、coach artifacts 诊断为 `provider_not_configured`、schema 通过且 `llm_runs` 可读回。真实 provider、登录 Cookie 和 Vite proxy 不在本轮范围。 |
| 2026-07-09 | AI 运行记录必须解释运行环境，而不是只暴露 raw fallback 码。 | 本地 `5173` 纯前端验收时，`/api/coach/artifacts` 没有真实后端处理，React 会记录 `local-fallback / server_unavailable`；如果 UI 只显示“降级原因”，用户会把本地前端模式误判成真实大模型失败，损害对 AI 能力的判断。 | React `LlmRunPanel` 新增诊断区，把运行记录分成本地前端未连接后端、schema 失败、真实 provider 成功、运行失败和规则降级，并显示恢复动作；`CoachPage.test.tsx` 覆盖本地前端模式诊断。仍未新增 dev server/API runtime smoke，也不代表远端 provider 质量闭环已完成。 |
| 2026-07-08 | 求职画像删除必须使用整包恢复，而不是只恢复单条子对象。 | 画像是 AI 教练上下文根节点，删除会级联知识边界、候选反馈、个人日程、AI 建议和 AI 运行记录；如果只恢复单条边界或日程，用户仍会失去个性化上下文和今日执行闭环。 | React 新增 `DeletedUserProfileBundle` 和 `restoreUserProfileBundle`，删除画像前保存本地快照，确认删除后在求职画像面板展示可读屏撤销提示；点击“撤销删除”会恢复画像本身、关联边界、候选反馈、日程、AI 草稿和运行记录，并让恢复画像重新成为当前画像、重建今日 sprint。账号禁用/恢复/删除和邀请删除仍属于服务端权限动作，必须先有审计日志和恢复规则，不能套用前端快照撤销。 |
| 2026-07-08 | 个人日程删除延续本地行动撤销模式。 | 个人日程会生成今日页行动，并可能承接已采纳 AI 日程草稿的完成归因；误删会丢失时间段、类型、原因、证据要求和 `acceptedFromArtifactId`，让用户看不清今日执行闭环。 | React 新增 `restoreCoachScheduleEvent`，确认删除后在“我的日程”顶部展示可读屏撤销提示；点击“撤销删除”会按原 ID、日期、时间、类型、原因、证据要求和 AI 草稿来源恢复原日程，并重建今日 sprint。只恢复仍有归属画像的本地日程；画像删除、账号禁用/恢复/删除和邀请删除仍需快照或审计日志规则，不能前端伪造撤销。 |
| 2026-07-08 | 知识边界删除延续本地上下文撤销模式。 | 知识边界是 AI 教练上下文骨架，会影响 AI 建议、知识卡、面试训练和后续候选题；误删后如果只能重新录入，会丢失来源摘要、置信度、provider、prompt version 和输入 hash。 | React 教练页新增 `restoreKnowledgeBoundary`，确认删除后在知识边界顶部展示可读屏撤销提示；点击“撤销删除”会按原 ID、创建时间、更新时间、来源摘要和 AI provenance 恢复原边界。只恢复仍有归属画像的本地边界；画像删除、账号禁用/恢复/删除和邀请删除仍需审计日志与恢复规则，不能前端伪造撤销。 |
| 2026-07-08 | 机会记录删除延续本地 Evidence 撤销模式。 | 机会记录会进入今日 Evidence Gate，并影响 AI 教练的机会/JD 信号、候选题和后续建议；误删一条公司、岗位或 HR 沟通信号，会让用户失去正在推进机会的上下文。 | React 机会页复用 `restoreEvidence`，确认删除后在机会反馈顶部展示可读屏撤销提示；点击“撤销删除”会按原 ID、时间、内容和任务恢复原 Evidence。当前只覆盖本地机会记录；账号禁用/恢复/删除、邀请删除等服务端或权限动作仍需完整审计日志和恢复规则，不能伪造为前端撤销。 |
| 2026-07-08 | 本机复盘证据删除必须提供短时撤销恢复。 | 复盘证据会影响今日 Evidence Gate 和周复盘归因；即使已有二次确认，误删后如果只能重新填写，用户会失去连续记录和原始时间线。 | React 新增 `restoreEvidence` store action，复盘历史删除本机记录后展示可读屏恢复提示；点击“撤销删除”会按原 ID、时间、内容和任务恢复 Evidence。当前只覆盖本地可恢复的复盘证据；账号删除、邀请删除和其它服务端动作仍需审计日志和恢复规则，不能直接伪造撤销。 |
| 2026-07-08 | 将本轮完整需求开发路径沉淀为复用模板。 | 用户要求把“需求澄清、产品闭环、数据隔离、UI/UX、开源安全、多端验证、GitHub 收口”的工作流归纳成后续需求可复用的模板，避免每次重新组织流程。 | 新需求默认先读 `docs/product/product-ops/requirement-development-template.md`，先填标准需求卡，再按“一页速用版”或 feature capsule 推进；产品级需求必须回填账本、已知问题和完成审计，普通用户路径、owner 管理路径、统计归属和破坏性动作确认默认前置检查；`validate:product-iteration` 已纳入该模板关键章节检查，防止后续删空或漏掉复用入口。 |
| 2026-07-08 | 建立每日主动产品迭代机制和 Codex heartbeat。 | 用户希望后续不再只按单次需求被动修补，而是每天由 Codex 主动探索不足、修复问题、优化交互并推动产品变好。 | 每日默认只选 1 个主任务，避免随机重构；机制先以 `daily-product-iteration.md`、`daily-product-iteration-log.md` 和 Codex heartbeat 承载，长期稳定性需要连续运行验证。 |
| 2026-07-06 | 选择“AI 原生求职教练叙事 + 收敛 MVP 工程边界”。 | 用户需要从个人工具扩展到泛 IT 求职者，吸引力来自个性化建议而非普通日程。 | 不做公开注册、组织租户、自动投递和自动 agent。 |
| 2026-07-06 | AI 建议必须是草稿，不能直接改正式数据。 | 降低幻觉和误改日程风险。 | 用户需要多一步接受/拒绝操作。 |
| 2026-07-06 | Java 后端保留为示例路径，不再作为产品主定位。 | 用户个人路径仍有价值，但产品主身份要面向泛 IT 求职者。 | 知识库中仍会保留部分 Java 示例，后续按角色族扩展。 |
| 2026-07-06 | 先补 11 个泛 IT 角色族 playbook 和首批角色追问题卡，再补完整题库和 JD 匹配。 | 单用户工具扩展到多用户后，AI 草稿必须先尊重角色差异；完整行业题库会让范围失控。 | 已覆盖角色视角、证据类型、回答框架、日程焦点、主候选题和追问库；仍不是完整岗位知识库，也不替代真实 LLM 线上 evidence。 |
| 2026-07-07 | 先把机会/JD 信号接入 AI 教练上下文，再补规则版结构化 JD 解析，最后做真实 LLM 深度解析和完整题库。 | 求职教练不能只根据画像和知识边界生成泛化建议；当前正在投递/沟通的公司、岗位、状态、JD 关键词和反馈必须影响知识卡、日程建议和候选题。 | React/Node/Rust 已能引用机会信号并纳入输入摘要 hash，并提取 `JD焦点`；本轮进一步补 `JD解析`，输出岗位责任、硬技能、风险信号、证据要求和候选追问，让建议围绕可面试能力点生成；这证明机会进展可影响下一轮建议，但仍不是自动投递、企业 ATS、真实 LLM 深度 JD parser 或真实 LLM 线上质量证明。 |
| 2026-07-07 | 产品扩展优先做“每个用户自己的 AI 教练上下文”，不是多人日历。 | 用户提出不同用户可添加日程、输入知识边界并由大模型动态输出知识卡、日程建议和候选题；吸引力来自个性化上下文闭环，不是多一个 CRUD 日程。 | 新增 `product-completeness-and-ai-expansion-2026-07-07.md` 明确当前不是完整公开产品；React/Node/Rust fallback 已从机会/JD 信号提取 `JD焦点` 和规则版 `JD解析`，让知识卡、日程建议和候选题围绕岗位责任、主技能、风险点和证据要求生成；AI 提取知识边界候选已落地为确认流，采纳后正式边界保留 provenance，React 已记录候选采纳/拒绝/修订反馈，且 Node/Rust 已新增 `/api/coach/boundary-feedback` 服务端长期反馈账本；仍需真实 LLM 远端证据、真实 LLM 深度 JD parser、批量初始化审计和服务端跨周归因。 |
| 2026-07-07 | 把产品迭代工作流变成自动门禁，而不是只写在复盘里。 | 用户要求以后产品迭代不需要一直盯着；流程文档如果不进测试和交付 readiness，会在下一次大改或最终交付时被绕过。 | 新增 `npm run validate:product-iteration` 和 `tests/product_iteration_workflow_test.js`，根 `npm test` 会阻断 PRD 多版本缺失、React UI 测试从根门禁移除、远端 coach evidence 缺角色追问库、已知外部限制未明示等退化；`npm run validate:delivery` 也会输出 `product_iteration_workflow` 独立检查项并在阻断问题存在时失败；DeepSeek provider 配置和远端 coach smoke 通过后，当前状态已升级为 `PASS`。 |
| 2026-07-07 | 成功指标必须出现在产品页，而不是只留在 PRD。 | 泛 IT 求职教练的价值要看“是否推进求职”，不是只看画像、草稿和运行次数；用户不应该翻文档才知道北极星指标。 | React Coach 页头部新增 `本周有效推进`、`采纳后完成`、`面试复盘`；`CoachDashboard` 统一计算已完成且有验证证据的任务数、AI 采纳日程完成率、带候选题任务的面试/口述/复盘完成率；`coachAdapter.test.ts` 和 `CoachPage.test.tsx` 已覆盖，产品迭代门禁也检查该决策文档。 |
| 2026-07-08 | 统计模块必须承接结果指标，而不是只做模块入口。 | 用户明确反馈统计分散在模块头部会显乱；Stats 页需要优先回答“今天是否推进、AI 建议是否执行、面试是否复盘、AI 运行是否可靠”。 | React `StatsPage` 首屏已集中展示今日完成、本周有效推进、采纳后完成、面试复盘、AI 运行和同步状态，并新增“结果闭环”“AI 运行质量”面板；`StatsPage.test.tsx` 使用有证据完成、采纳 AI 日程、面试复盘和 success/fallback/failed LLM 运行数据覆盖。 |
| 2026-07-08 | More 页不再承载重复 KPI 卡片。 | More 页的主要任务是账号、同步、备份和低频入口；如果继续堆完成、证据、延期、画像、AI 建议等卡片，会和 Stats 页职责冲突，也会让普通用户误以为“更多”才是统计页。 | React `MorePage` 头部已改为低密度“统计快照”，只保留执行闭环、画像资产、同步摘要和单一“查看集中统计”入口；普通账号卡和后续能力入口不再重复放统计链接；`MorePage.test.tsx` 覆盖旧 KPI 标签消失和新入口存在。 |
| 2026-07-08 | 业务模块头部不再放“集中统计”卡片。 | 画像、知识、面试、机会、复盘页应该优先回答“当前模块要做什么”，而不是在每个头部重复推一张统计卡；统计模块已经是主导航一级入口。 | React 已移除 `CoachPage`、`LearningPage`、`InterviewPage`、`ApplicationsPage`、`ReviewPage` 头部的 `/stats` 大卡片；`navigationRoutes.test.tsx` 覆盖 5 个业务模块不再出现“集中统计”，`CoachPage.test.tsx` 同时验证全局导航仍保留 `#/stats` 入口。 |
| 2026-07-08 | 创建/编辑/保存入口必须说明当前状态和保存影响。 | 用户反馈创建时交互不友好、编辑保存没有提示；保存类表单如果不说明“新增还是编辑”和保存后会更新什么，用户会担心误改已有数据或不知道 disabled 原因。 | React 教练页知识边界、我的日程、机会页机会记录、面试页口述训练入口、学习页学习笔记入口、复盘页服务端结果快照入口和本地复盘表单已新增模式/保存影响提示；日程编辑新增取消编辑动作，知识边界和日程均覆盖“编辑 -> 取消 -> 回到新增态”的页面测试，机会页覆盖新增写入 Evidence Gate/AI 信号和编辑更新证据，面试页覆盖空回答 disabled 原因和保存写入 Evidence Gate，学习页覆盖空笔记 disabled 原因和保存写入当前知识任务 Evidence Gate，复盘页覆盖本地模式禁用服务端快照、空复盘 disabled 原因、新增保存影响和编辑覆盖影响。 |
| 2026-07-08 | 破坏性操作必须先解释影响并允许取消。 | 删除本机复盘证据会改变今日 Evidence Gate，删除求职画像会级联清理知识边界、个人日程和 AI 建议，删除知识边界会影响后续 AI 建议/知识卡/面试训练，删除个人日程会让今日页不再展示行动，删除机会记录会移除 JD/沟通信号，删除邀请记录会让 owner 看板不再展示试用登记但不会自动删除登录账号；单个或批量登录账号禁用、恢复、删除会改变用户登录权限，但不会自动删除数据域、邀请记录和历史求职数据；如果按钮一次点击就直接执行，用户无法确认影响范围，也很难判断是否误操作。 | React 复盘历史已抽出 `LocalReviewRecords` 组件并补二次确认；React 求职画像、知识边界、个人日程、机会记录、邀请记录删除、单个登录账号禁用/恢复/删除和批量账号禁用/恢复/删除均已改为面板内确认区，首次点击只展示影响说明，用户可取消，确认按钮才调用动作；`ReviewPage.test.tsx`、`CoachPage.test.tsx`、`ApplicationsPage.test.tsx`、`InviteManagementPanel.test.tsx` 和 `InviteManagementBatchAccountActions.test.tsx` 覆盖首次点击不执行、取消和确认后执行。 |
| 2026-07-07 | 周结果归因必须服务端可读写快照。 | 北极星指标不能只停留在 React 本地周复盘，否则换设备、换用户或做长期趋势时无法复用；后续真实 LLM 周复盘也需要一个可持久的输入和输出锚点。 | Node/Rust 新增 `/api/coach/outcomes`，GET 计算有效推进、采纳后完成、面试复盘、延期和证据分布，POST 写入 `coachOutcomeSnapshots`；React Review 页新增“服务端周结果归因”和“保存服务端快照”；`node tests/api_runtime_test.js`、`node tests/node_ai_routes_boundary_test.js`、`node tests/rust_ai_routes_boundary_test.js`、Rust `runtime_contract` 和 `ReviewPage.test.tsx` 已覆盖。仍不等于真实 LLM 周复盘或机会/面试结果长期归因。 |
| 2026-07-07 | AI 提取知识边界只能生成候选，不能直接改正式边界。 | 知识边界是 AI 教练上下文骨架，错误写入会污染后续知识卡、日程建议和候选题；私人工具必须保持用户可控。 | React 已新增“AI 提取边界”确认流，Node/Rust 新增 `/api/coach/boundary-suggestions`，服务端不可用时 React 使用本地规则 fallback；已覆盖跨角色通用技术主题提取、已保存主题过滤、权限、schema、HTTP runtime、Anthropic-compatible provider 成功、provider timeout fallback 和采纳后 provenance。 |
| 2026-07-07 | 被采纳的 AI 边界必须保留来源。 | 用户后续需要知道一条知识边界来自 JD、简历、面试反馈还是本地 fallback；否则 AI 上下文会变成黑箱，后续复盘也无法判断边界质量。 | `KnowledgeBoundary` 已保留来源摘要、置信度、provider、prompt version 和输入 hash；`CoachPage.test.tsx`、`coachBoundaryProvenance.test.ts`、`coachAdapter.test.ts` 和 `boundarySuggestionAdapter.test.ts` 已覆盖本地候选生成、采纳和正式边界显示。仍需真实 LLM 远端 evidence 和服务端长期归因。 |
| 2026-07-07 | 边界候选反馈从 React runtime 升级为 Node/Rust 服务端长期账本。 | 候选被拒绝或需要修订时，也是在校准用户知识边界；如果只记录采纳，会让 AI 误以为所有生成都有效。 | React 继续记录 `accepted`、`rejected`、`needs_revision` 三类反馈、反馈原因、provider/prompt/input hash，并进入 runtime sync、More 导出/导入和校准摘要；Node fallback 会写入 `progress.coachBoundaryFeedback` 与 `progress.coach.boundarySuggestionFeedback`，Rust 写入独立 `coach_boundary_feedback` 表并返回 `topTopics`、`recentReasons` 和 `nextExtractionHint` summary；远端真实 LLM 质量归因和批量初始化审计仍待补。 |
| 2026-07-07 | 先补邀请制首登编排、批量素材包和最小邀请台账，再做可登录账号开通后台。 | 用户要求不同用户能输入自己的知识边界并得到个性化建议，新用户首登必须从账号/数据域、画像模板、素材边界、首条日程和 AI 草稿确认形成可见路径；owner 也需要知道哪些用户已登记、已邀请、已激活。 | React 已有 1/5 到 5/5 首登编排、11 个角色族模板、JD/简历/面试反馈/学习笔记素材段追加和素材识别；Node/Rust 已有首登观察事件写入和读回；React/Node/Rust 已有只读邀请批次首登聚合报表；React/Node/Rust 已补 `/api/coach/invitations` 邀请台账和管理面板；users file 账号开通/重置已有本地合同和远端 smoke；本轮已补批次筛选、批量邀请导入、批量状态更新、邀请报表 JSON 导出、首登模板版本和邀请记录删除。 |
| 2026-07-07 | 邀请账号管理从纯台账升级为 users file 账号生命周期。 | 私人工具扩展到多人试用时，登记用户后必须能完成首登，也必须能收回登录权限；但仍要避免公开注册和仓库内写密钥。 | 仅在服务端配置仓库外 `JOB_SPRINT_USERS_FILE` 且未使用 `JOB_SPRINT_USERS_JSON` 时启用；Node/Rust 会写入 password hash，不在 API 响应中回显密码或 hash；本地合同覆盖批量邀请导入、开通、重置、禁用、恢复、删除、新用户登录、旧密码失效和 runtime 隔离，远端 smoke 已覆盖 `codex-smoke-account` 登录、session 读回、owner 隔离、禁用后拒绝登录、恢复后可登录、删除后拒绝登录；React 已提供可复制邀请通知草稿。仍需外部 SMTP/IM 自动发送和更完整的用户管理后台。 |
| 2026-07-07 | 批量账号动作必须操作真实登录账号，而不是只改邀请状态。 | 小范围试用会同时邀请一个批次的人；owner 需要一键暂停、恢复或清理批次账号，不能逐个点击，也不能把“批量邀请状态”误当成登录权限变更。 | React 邀请账号管理已新增批量账号动作控件，仅在选中具体邀请批次且存在已配置账号时可执行；批量禁用、恢复、删除首次点击只展开确认区，确认文案明确批次、账号数、登录权限影响和数据保留边界，确认前不会调用服务端；Node/Rust 新增 `account-batch-status`，支持按用户名或批次批量禁用、恢复、删除 users-file 登录账号，保护 owner/current 账号，跳过不存在账号；本地 Node/Rust 合同和远端 `coach-invitation-account.json` 已覆盖批量禁用后登录 401、批量恢复后登录成功、批量删除后登录 401，响应不泄露密码/hash；React `InviteManagementBatchAccountActions.test.tsx` 覆盖首次点击不执行、取消不执行和确认后调用批量账号动作。 |
| 2026-07-07 | 登录入口先做可复制邀请通知草稿，不冒充外部发送已完成。 | 私人工具小范围试用需要 owner 能把登录入口、安全提醒和单独发送密码的说明交给用户；但 SMTP、企业 IM 或短信 provider 未接入时，不能把草稿误报为自动发送。 | React 邀请账号管理可选择邮件、IM 或手工渠道生成通知草稿；Node/Rust 新增 `notification-draft`，仅为已配置且未禁用的用户生成草稿，跳过不可登录账号；本地合同和远端 `coach-invitation-account.json` 覆盖 `invitation_notifications_generated=2`，并断言响应不泄露明文密码或 sha256 hash。 |
| 2026-07-07 | UI 级登录切换必须独立证明。 | API 登录成功不能证明用户真的能通过页面退出、切换账号并看到正确数据域；邀请制小范围试用最怕 owner 开了账号但用户入口不可用。 | 新增 `write_remote_login_switch_evidence.js` 和 `remoteLoginSwitch` 命令；远端 `login-switch.json` 已通过真实登录页 owner 登录、页面退出、smoke 用户登录、session/dataScope 读回和账号清理。 |
| 2026-07-07 | 首登可观测必须服务端可记录，但不自动静默上传。 | 团队不能只知道“有没有向导”，还要知道用户卡在账号、画像、素材、日程还是 AI 草稿确认；同时私人工具要避免用户不知情的后台埋点。 | React 首登面板显示完成率、当前放弃点、风险等级和下一步动作，并提供“记录首登观察”；Node/Rust `/api/coach/onboarding-events` 已支持 POST/GET、摘要和持久化；邀请批次聚合、最小邀请台账、批量邀请导入、批量状态更新、报表导出、模板版本和 users file 账号生命周期已补，本地合同和远端账号生命周期 smoke 已通过。 |
| 2026-07-08 | 批次首登看板必须支持运营筛选，而不是只展示前几条。 | owner 需要优先看高风险批次、共同放弃点和具体批次用户；只 `slice` 前 4 个批次或前 5 个用户会让风险用户被隐藏。 | React `InviteOnboardingReportPanel` 已新增批次、风险、放弃点筛选，当前筛选计数、批次/用户展开收起和空状态；`InviteOnboardingReportPanel.test.tsx` 覆盖折叠提示、展开、风险筛选、放弃点筛选、批次筛选和空结果。服务端聚合口径未变。 |
| 2026-07-08 | 管理员中心必须分区承接 owner 能力，而不是把看板和账号动作堆在一个长页。 | 用户已经明确指出“更多模块更像管理员看的”和邀请用户不该混进画像模块；当 owner 能力继续增加时，首登风险看板和账号生命周期动作如果同屏堆叠，会让普通产品路径和管理路径再次混乱。 | React `AdminPage` 已改为 owner-only `tablist`，默认进入“建档看板”，账号登记、开通、禁用、恢复、删除和通知草稿只在“账号管理”工作区加载；`AdminPage.test.tsx` 覆盖 owner tab 切换和两个面板互斥，既有 `navigationRoutes.test.tsx` 覆盖普通用户直达 `/admin` 会回到 More。后续用户管理后台仍需账号详情页、搜索筛选和外部 SMTP/IM 自动发送。 |
| 2026-07-08 | 账号管理台账必须先支持搜索筛选，再考虑更重的详情页。 | owner 找某个试用用户时最常用的信息是登录名、显示名、数据域、岗位和备注；如果只能按批次过滤并展开长列表，会继续形成管理后台长页压力，也容易误选批量账号动作对象。 | React 新增 `InviteManagementFilters`，把批次筛选、账号搜索和当前计数组件化；搜索会同时过滤邀请记录、登录账号、批量账号动作和邀请通知生成范围，并用 helper 文案说明影响；`InviteManagementPanel.test.tsx` 覆盖无需展开即可搜到第 9 条邀请和第 7 个账号，清空搜索后恢复折叠列表；`InviteManagementPanel` 回落到 533 行，继续满足架构行数门禁。账号详情页、外部 SMTP/IM 和完整用户管理后台仍未完成。 |
| 2026-07-08 | 账号管理需要轻量详情面板承接单账号上下文，而不是只把点击行变成编辑表单。 | 搜索解决了“找得到”，但 owner 点中某个账号后仍缺少一个稳定的只读上下文区域；如果点击行只把左侧表单填上，用户很难判断当前是在查看登录账号、邀请记录、数据域、批次还是准备编辑保存。 | React 新增 `InviteManagementDetailPanel`，右侧在台账上方显示空状态、登录账号详情、邀请记录详情和关闭动作；点选账号会展示登录状态、角色、登录名、数据域、批次和操作提示，点选邀请记录会展示状态、岗位、角色族、模板版本和运营备注；`InviteManagementDetailPanel.test.tsx` 覆盖空状态、选中账号、选中邀请和关闭详情。仍未做独立账号详情页、最近动作时间线或服务端详情接口。 |
| 2026-07-08 | 账号详情必须承接首登风险摘要，而不是只展示账号字段。 | 轻量详情面板已经解决“点选后看什么”，但 owner 真正要判断的是试用用户是否卡在首登、当前风险和下一步动作；如果详情只显示登录名、批次和备注，仍不能闭环处理新用户为什么没有完成建档。 | React `AdminPage` 复用既有 `onboardingReport` 传给 `InviteManagementPanel`，新增 `inviteManagementOnboarding` helper 按 username 或 dataScope 匹配报表用户；`InviteManagementDetailPanel` 展示首登状态、完成率、风险、放弃点和下一步，无匹配时给出可恢复空状态；`InviteManagementDetailPanel.test.tsx` 覆盖有匹配摘要和无匹配 fallback。仍未做最近动作时间线、独立账号详情页或服务端账号详情接口。 |
| 2026-07-08 | 账号生命周期动作必须进入服务端审计账本。 | owner 需要知道开通、重置、禁用、恢复、删除和批量账号动作是谁触发、影响了哪些账号；如果只展示当前 users file 状态，无法解释账号为什么变成当前状态，也无法支撑后续恢复规则。 | Node/Rust users-file 模式新增 `accountAuditEvents`，成功开通、重置、单账号禁用/恢复/删除和批量账号动作都会追加只含元数据的审计事件，不记录密码、hash 或 session；`/api/coach/invitations` 会读回最近审计，React 账号详情最近动态优先展示服务端审计，再 fallback 到首登和台账状态；Node/Rust 合同和 React 详情测试已覆盖。独立账号详情页、按事件筛选审计后台、账号恢复规则和外部 SMTP/IM 自动发送仍未完成。 |
| 2026-07-08 | 账号详情里的审计历史必须可筛选，并同步说明恢复规则。 | 最近动态只能解释“发生过什么”，但 owner 真正处理账号时还需要按开通/重置、禁用/恢复、删除/清理和批量动作快速定位证据，并知道删除、禁用和重置分别能否恢复。 | React `InviteManagementDetailPanel` 新增账号审计类型筛选、匹配计数和恢复规则提示；`inviteManagementTimeline` 抽出审计匹配和筛选 helper，筛选时仍只匹配当前账号或受影响用户名，避免其他用户审计串入详情；`InviteManagementDetailPanel.test.tsx` 覆盖多动作审计、其他用户噪音隔离、删除筛选和删除恢复规则。独立账号详情页、服务端审计专页、真实恢复动作编排和外部 SMTP/IM 自动发送仍未完成。 |
| 2026-07-08 | 账号详情必须从“看审计”走到“处理下一步”。 | owner 查到账号审计后，如果还要回长列表里找同一个账号再禁用、恢复或删除，会形成二次定位和误操作风险；完整用户后台之前，详情面板至少要承接搜索审计和单账号下一步动作。 | React 账号详情的审计区新增搜索、展开全部和无结果提示，默认只展示前 5 条但可查看全部匹配记录；登录账号详情新增“账号下一步”操作区，基于当前状态提供禁用或恢复，并保留删除登录账号，所有动作先显示影响说明和确认按钮，再复用既有 `updateCoachInvitationAccountStatus`；`InviteManagementDetailPanel.test.tsx` 覆盖审计展开、审计搜索、其他用户隔离和详情内禁用动作确认后才调用 API。仍不等于独立用户详情页、服务端审计专页、自动恢复流程或外部通知发送。 |
| 2026-07-08 | 账号详情的开通/重置只能准备表单，不能绕过新密码输入。 | 密码不能回显、不能恢复旧值，也不能从详情按钮直接触发重置；owner 需要从详情上下文一键切到开通/重置模式，但必须明确输入新密码并保存。 | React 账号详情“账号下一步”新增“开通/重置密码”，点击后只把左侧邀请表单切到 `provisionAccount=true`、清空密码输入、设置账号状态为 active，并提示必须输入新密码；确认保存仍走既有邀请表单和 `saveCoachInvitation`，不会调用单账号状态 API，也不会显示旧密码。`InviteManagementDetailPanel.test.tsx` 覆盖点击后勾选开通/重置、清空旧密码输入、显示提示且不调用 `updateCoachInvitationAccountStatus`。仍不等于公开注册、自动恢复旧密码、外部 SMTP/IM 发送或独立账号详情页。 |
| 2026-07-08 | 账号详情改为右侧抽屉，台账常态只负责定位和筛选。 | 详情空态常驻在台账上方会浪费 owner 扫描空间；选中账号后，详情、审计和下一步动作应该形成临时上下文层，关闭后回到当前筛选和台账位置。 | React `InviteManagementDetailPanel` 改为 `dialog` 抽屉，未选中时不占台账空间；点选账号或邀请记录后打开右侧抽屉，台账行显示“详情已打开”选中态，Esc 或关闭按钮可退出；筛选区改为独占一行，批量说明和按钮下移，避免桌面宽屏被按钮挤压成窄列。`InviteManagementDetailPanel.test.tsx` 覆盖未选中无抽屉、点选打开、选中态和关闭消失；Playwright 视觉 smoke 生成 `/tmp/job-sprint-account-detail-drawer.png`，确认抽屉和筛选布局正常。仍不是独立账号详情路由、审计专页或完整用户管理后台。 |
| 2026-07-08 | 账号详情抽屉需要移动端底部 sheet 形态。 | Android WebView 和手机浏览器里，桌面右抽屉会显得像全屏硬弹窗；移动端更适合从底部承接详情，保留标题、关闭和内部滚动，避免遮挡底部导航后仍让用户找不到退出路径。 | React 账号详情容器改为响应式：`sm` 以上继续右侧抽屉，小屏改为底部 sheet，最大高度约 `88dvh`，顶部有 sheet 把手，内容区加入 safe-area bottom padding；关闭按钮和 Esc 逻辑不变。Playwright 视觉 smoke 覆盖 1440 桌面和 390 移动视口，输出 `/tmp/job-sprint-account-detail-drawer-desktop.png` 与 `/tmp/job-sprint-account-detail-sheet-mobile.png`，证明桌面右抽屉和移动底部 sheet 同时可用。仍不代表 Android 内置 React assets 已同步或远端 evidence 已刷新。 |
| 2026-07-08 | 账号详情关闭后必须把焦点退回触发行。 | 抽屉和底部 sheet 解决了空间和响应式问题，但键盘或读屏用户关闭详情后如果焦点丢到页面顶部或 body，会失去刚才筛选和点选的位置；这会破坏 owner 在长台账里连续查看账号的工作流。 | React 邀请管理父组件记录打开详情时的台账按钮，关闭详情时恢复焦点到仍存在的触发行；`InviteManagementLedger` 将点击事件的 `currentTarget` 传给父组件；`InviteManagementDetailPanel.test.tsx` 覆盖关闭邀请详情后焦点回到 `Candidate 1` 台账按钮。仍不新增完整焦点陷阱、拖拽关闭或独立账号详情页。 |
| 2026-07-06 | 产品迭代文档采用 feature capsule + product-ops 长期账本。 | 防止 PRD、验收、复盘散落，降低下次迭代依赖用户盯梢。 | 每次收口需要额外同步文档。 |
| 2026-07-06 | 多用户先验 Node `dataScope` 与 Rust/SQLite `scope` 隔离，再做公开注册或组织租户。 | 当前目标是邀请制泛 IT 求职者，不需要过早做 SaaS 账号系统。 | 可以证明用户数据不串线，但还不能宣称公开多租户产品。 |
| 2026-07-06 | AI 草稿生成先补服务端 `/api/coach/artifacts` 合同和用户态 AI 运行记录，再补 `llm_runs` / `llm_feedback` 服务端账本。 | 用户需要看到个性化 AI 建议，也需要知道每次生成来自服务端、fallback 还是失败；采纳/拒绝必须反哺下一轮提示词，而不是只停在前端统计。 | Node 与 Rust 均可接 Anthropic-compatible provider；React 已记录 provider、model、prompt version、schema version、输入摘要 hash、schema 结果和 fallback 状态，并 best-effort 提交接受/拒绝反馈；Node/Rust 已返回 feedback summary，远端 smoke 已读回 `reviewedCount=1` 和 `qualityLabel=偏离目标`；Rust 本地 mock provider 合同已覆盖 token、延迟和可选成本，但仍不能宣称远端真实 LLM 和服务端/跨周采纳后完成率归因已完成。 |
| 2026-07-07 | Android remote evidence 不能只看顶层 `webViewUrl`。 | WebView 远端加载失败时可能回落到 `file:///android_asset/...`，如果报告只记录配置 URL，会把本地功能误判成正式远端通过。 | Android remote 脚本现在要求页面、flow/restart 快照停留在配置的 HTTPS origin 且包含 `/job-sprint/`；readiness 还会检查 auth session 状态 URL，发现本地 fallback 直接 FAIL。 |
| 2026-07-06 | 先补 React 本地日程级采纳后完成率归因，再延期服务端/跨周归因。 | PRD 的“AI 建议被采纳并完成”不能只看采纳按钮；用户接受 AI 日程草稿后，系统必须能知道它是否真的变成今日完成任务。 | React dashboard 已从 `acceptedFromArtifactId` 找到对应 `coach-event-*` 任务，并在 AI 反馈复盘展示采纳日程完成率和执行判断；这证明本地今日闭环，但不替代服务端跨日/周复盘归因。 |
| 2026-07-06 | 先补 React 本地规则版复盘 AI 分析，再延期真实 LLM 周复盘。 | 用户反馈过“复盘后由 AI 分析哪里欠缺”，复盘页不能只提示“可提交给 AI”，必须直接产出事实、欠缺和下一步动作。 | Review 页已基于 Evidence Gate、复盘记录、薄弱回答、路径问题和 AI 建议反馈生成本地规则版分析；这证明单日复盘可用，但不替代服务端真实 LLM、跨周归因和机会结果归因。 |
| 2026-07-06 | 先补 React 本地 7 日周复盘归因，再补服务端周结果快照。 | PRD 的北极星指标是“每周有效求职推进数”，复盘页必须能把一周证据、机会反馈、延期、AI 反馈和完成任务合成可行动判断。 | Review 页已新增“周复盘归因”，输出闭环分、有效信号、结果风险和下周焦点；后续已补 `/api/coach/outcomes` 服务端周结果归因快照。这证明周级结果已有最小服务端锚点，但不替代服务端真实 LLM 和长期机会/面试结果归因。 |
| 2026-07-06 | 最新交付证据必须绑定 hash，不沿用旧报告。 | 大范围改动会重建 Rust ELF、server delivery manifest 和正式 APK；旧 hash 会误导后续判断。 | 当前服务器同步 manifest SHA-256 为 `e5a1d8195791d988b5e50a0c9badc310771d303dfe34a63167629f5919ae53f2`，Linux x86_64 Rust ELF SHA-256 为 `a785bef602c15959f77bdc08957e1de378d132d20ce55dc6a1d9ca5e21d0a89f`，正式 APK SHA-256 为 `221c7483d288c5642304d51786fe6f8a664046c2079d3f8ad2df1dcc281d8343`；远端 React 入口、coach invitations、coach account lifecycle、批量账号动作、邀请通知草稿、DeepSeek coach smoke、`JD焦点` smoke、规则版 `JD解析` 与角色追问库 smoke、boundary suggestions smoke 和登录态 `/api/coach/boundary-feedback` GET 已通过，`validate:delivery` 仍因 Android HTTPS remote evidence 缺口失败。 |
| 2026-07-06 | React 多画像先按激活画像隔离今日自定义日程，并把 React typecheck/Vitest 接入根 `npm test`。 | “每个用户自己的日程”不能只停在 Coach 页过滤；今日任务合成也必须按当前作用域过滤，且 UI 交互测试要进入主门禁。 | 这证明本地多画像不串今日日程，但仍不是公开注册/组织租户；根测试耗时增加约数秒，换取 UI 回归进入默认验收。 |
