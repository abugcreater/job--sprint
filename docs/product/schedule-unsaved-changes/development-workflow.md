# 个人日程未保存修改保护开发与验证流程

日期：2026-07-30

1. 记录日程草稿的独立基线，并实现八字段 dirty 判断。
2. 将“请求替换”和“真正替换草稿”拆开；仅 dirty 时进入确认态。
3. 在个人日程面板内渲染可访问的确认框，复用现有按钮和视觉语义。
4. 用页面测试覆盖新建切换、已有编辑取消、继续编辑、放弃修改、保存后的快速取消。
5. 执行类型检查、全量前端测试、构建、根门禁、产品迭代校验和敏感扫描。
