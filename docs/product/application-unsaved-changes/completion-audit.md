# 机会编辑未保存修改保护完成审计

日期：2026-07-26
状态：PASS_WITH_LIMITS

| 验收项 | 结果 | 证据 |
|---|---|---|
| 新增机会的未保存输入不会直接丢失 | PASS | `ApplicationsPage.test.tsx` 修改公司和岗位后点击“取消新增”，断言出现确认；“继续编辑”后两个输入仍保留；“放弃修改”后未新增 Evidence Gate 记录。 |
| 已有机会不会被未保存编辑覆盖 | PASS | `ApplicationsPage.test.tsx` 修改已有记录的沟通反馈后从移动端关闭按钮退出，放弃后仍读到原反馈，不读到修改文本。 |
| 未修改退出保持快速路径 | PASS | 同一页面测试在未改动已有机会时点击“取消编辑”，断言没有确认面板。 |
| 草稿比较覆盖标签且不共享数组 | PASS | `applicationsAdapter.test.ts` 覆盖克隆标签数组和 dirty 判断，避免基线被编辑过程意外修改。 |
| 桌面与窄屏确认面板可用 | PASS | 本地浏览器以默认桌面与 `390 × 844` 视口走查：确认面板完整显示、继续编辑保留公司输入、放弃后编辑器和确认面板均关闭，控制台无 error。 |
| 仓库与前端回归 | PASS_WITH_LIMITS | `npm test`、产品迭代门禁、敏感扫描和 GitFlow `verify` 通过；React 为 37 个文件、120 个测试通过，生产构建通过但保留既有 500 kB 分包警告。 |

限制：本审计只覆盖机会编辑器的“取消编辑”和移动端关闭按钮；不证明浏览器刷新、关闭标签页、应用强杀、Android 系统返回或全部外部路由跳转均已拦截。未实现全局表单协议、自动保存、服务端草稿、服务器交付或 Android 更新。
