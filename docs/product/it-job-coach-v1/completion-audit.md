# 本次目标完成审计

日期：2026-07-07

## 审计结论

当前结论：`PASS_WITH_LIMITS`。

产品定位、PRD 多版本、推荐 PRD、首批泛 IT 角色族、可复用工作流、本地可验证的 AI 教练最小闭环、Node/Rust 服务端多用户 coach 数据隔离、React 当前账号/数据域可见性、`/api/coach/artifacts` 服务端草稿生成合同、`/api/coach/boundary-suggestions` 知识边界候选提取合同和 Anthropic-compatible provider/fallback 路径、React 候选边界采纳后 provenance 持久化、React 候选反馈本地账本、Node/Rust `/api/coach/boundary-feedback` 服务端边界反馈账本、React 邀请制首登编排、React 首登完成率/放弃点/风险/下一步可观测、React Coach 页 `本周有效推进`/`采纳后完成`/`面试复盘` 结果指标、Node/Rust `/api/coach/onboarding-events` 首登观察事件、Node/Rust `/api/coach/onboarding-report` 邀请批次首登聚合、Node/Rust `/api/coach/invitations` 邀请台账、Node/Rust `/api/coach/outcomes` 服务端周结果归因快照、批量邀请导入、users file 账号开通/密码重置/禁用/恢复/删除、批量账号禁用/恢复/删除、可复制邀请通知草稿、远端 UI 登录切换、邀请批次状态动作、邀请报表 JSON 导出、首登模板版本、邀请记录删除、React 邀请批次首登看板、React 邀请账号管理面板、React 首次配置准备度、React 快速初始化、React 批量素材包、React 角色族首登模板、React AI 反馈复盘、React 本地日程级采纳后完成率归因、React 本地规则版复盘 AI 分析、React 本地 7 日周复盘归因、React Review 页服务端周结果归因读写入口、11 个角色族 playbook、机会/JD 信号和规则版结构化 JD 解析、Rust/SQLite 独立 `llm_runs` 审计表、`llm_feedback` 服务端反馈账本、feedback `summary` 聚合和用户态 AI 运行记录已经落地。最新验证中 `npm test`、React typecheck/Vitest/build、Rust clippy/cargo test、Rust/SQLite UI 功能测试、Android 本地 WebView 功能测试、Linux x86_64 Rust binary 构建、public-safe、server-delivery、服务器同步、`job-sprint.service` 重启、服务器 HTTP 远端验收、远端 React 入口、远端邀请台账 smoke、远端邀请账号生命周期/批量账号动作/邀请通知草稿 smoke、远端 UI 登录切换 smoke、远端 DeepSeek coach artifact/feedback/outcomes smoke、远端 `JD焦点` smoke、远端 boundary suggestions smoke 和正式签名 APK 均通过。限制是：当前线程处于 `current_thread_quarantine=true`，不能声称完整多 agent 团队真实参与；React 已补最小邀请制首登编排、首登可观测、服务端首登观察写入/读回、只读邀请批次首登聚合、邀请台账、批量邀请导入、users file 账号开通/重置/禁用/恢复/删除、批量账号禁用/恢复/删除、邀请通知草稿、远端账号生命周期 smoke、远端 UI 登录切换 smoke、批量素材包、快速初始化、11 个角色族首登模板、批次状态动作、导出 JSON、模板版本、邀请记录删除、结果指标可视化和服务端周结果归因快照，但外部 SMTP/IM 自动发送和完整用户管理仍未完成；Rust 已有本地 mock provider、token、延迟和可选成本合同证据，Node/Rust boundary suggestions 已有 mock provider 成功和超时 fallback 证据，React 已证明采纳后边界保留来源摘要、置信度、provider、prompt version 和输入 hash，并已记录候选采纳/拒绝/修订反馈，Node/Rust 已补服务端长期边界反馈账本和 summary，Node/Rust 已补最小服务端周结果归因快照，远端真实 LLM provider evidence 已通过；但真实 LLM 深度 JD parser、完整岗位题库匹配、批量初始化审计、真实 LLM 周复盘、机会/面试结果长期归因、公开注册/组织租户和完整 HTTPS 生产交付仍未完成；Android 远端真机验收被 HTTPS 前置条件阻塞，当前公网 HTTP 服务可用，HTTPS 域名 DNS 和服务器本机证书/Nginx 正常，但外部公网 443 握手仍 EOF，Android 真机手动切 HTTPS URL 后首屏失败为 `net::ERR_CONNECTION_RESET`。

## 原始目标逐项审计

| 原始要求 | 当前证据 | 结论 |
|---|---|---|
| 产品定位改成面向求职者，当前专注泛 IT 人士 | `prd-recommended.md`、`docs/core/01-project-background.md`、React 顶部文案均已改为泛 IT AI 求职教练 | `PASS` |
| 充分审视现有产品是否完整闭环 | `review-and-adjudication.md` 和 `product-completeness-and-ai-expansion-2026-07-07.md` 已从产品、技术、UI、QA 和 PM 完整性角度指出闭环、风险和非目标 | `PASS_WITH_LIMITS`，真实多 agent 受 quarantine 限制 |
| 创建多个 PRD 版本 | `prd-options.md` 包含个人闭环版、泛 IT 多用户版、AI 原生教练版 | `PASS` |
| 团队讨论、审核后得到推荐版 | `review-and-adjudication.md` 裁决采用 `prd-recommended.md` | `PASS_WITH_LIMITS`，团队状态为 `TEAM_ROOM_PARTIAL` |
| 团队 leader 安排大范围产品迭代 | `development-workflow.md` 和 `product-ops/iteration-workflow.md` 定义阶段、角色、命令和门禁 | `PASS` |
| 避免关闭智能体异常导致任务失败 | `doc-rules.md`、`development-workflow.md` 和团队协议均要求 quarantine 时不等待/关闭 agent | `PASS` |
| 避免 UI 更新后功能不可用 | React Coach 页、学习、口述、延期、资料、面试复盘、账号状态和数据域展示等功能已纳入 Web/Android 本地测试；新增长列表展开、AI 反馈复盘、复盘页本地规则版 AI 分析、本地 7 日周复盘归因、服务端周结果归因区域、AI 提取边界候选确认流、React 多画像日程隔离测试；根 `npm test` 已接入 React typecheck 和全量 Vitest | `PASS_WITH_LIMITS`，远端 HTTPS Android 仍未验收 |
| 产品向预期方向发展 | 已实现画像、知识边界、自定义日程、AI 草稿、接受/拒绝、AI 反馈复盘、Coach 页 `本周有效推进`/`采纳后完成`/`面试复盘` 结果指标、邀请制首登编排、首登完成率/放弃点/风险/下一步可观测、服务端首登观察写入/读回、邀请批次首登聚合看板、邀请账号管理面板、批量邀请导入、users file 账号开通/重置/禁用/恢复/删除、可复制邀请通知草稿、远端 UI 登录切换 evidence、邀请批次状态动作、邀请报表 JSON 导出、首登模板版本、邀请记录删除、首次配置准备度、快速初始化、批量素材包、11 个角色族首登模板、今日页联动、`/api/coach/artifacts` 服务端生成接口、`/api/coach/boundary-suggestions` 边界候选提取接口、`/api/coach/boundary-feedback` 边界候选反馈接口、`/api/coach/outcomes` 服务端周结果归因快照接口、`/api/coach/invitations` 邀请台账、AI 运行记录和服务端反馈账本；React 会优先调用服务端，失败时提示本地规则 fallback，并记录 provider、model、prompt version、schema version、输入摘要 hash、token、延迟、可选成本和 fallback 状态；React/Node/Rust 已接入 11 个角色族 playbook，AI 草稿会带角色视角、证据类型、回答框架、日程焦点和候选追问；React/Node/Rust 已把机会/JD 信号升级为规则版结构化 JD 解析，知识卡、日程建议和候选题会引用岗位责任、主技能、风险点、证据要求和候选追问；React 接受/拒绝会 best-effort 写入 `/api/coach/feedback`；React 首登面板可手动提交 `/api/coach/onboarding-events`，Node/Rust GET 会返回最新完成率、放弃点、最高风险、下一步和首登状态；React 邀请批次看板可读取 `/api/coach/onboarding-report`，Node/Rust 会按 `inviteBatch` 返回邀请用户、开始数、完成数、完成率、最高风险、Top 放弃点和用户最新状态，owner 可看全量，普通用户只看自己的 `dataScope`；React 邀请账号管理可读取、保存和批量导入邀请台账，按批次筛选并批量更新状态，生成邀请报表 JSON，记录首登模板版本，删除邀请记录，并按邮件/IM/手工渠道生成邀请通知草稿；配置仓库外 `JOB_SPRINT_USERS_FILE` 时可开通、重置、禁用、恢复和删除登录账号，Node/Rust 合同覆盖新用户登录、旧密码失效、禁用后拒绝登录、恢复后可登录、删除后拒绝登录、通知草稿不泄露密码/hash 和 runtime 隔离，远端 smoke 覆盖同一生命周期与 `invitation_notifications_generated`，`login-switch.json` 已覆盖 owner UI 登录、退出、切换 smoke 用户登录和 dataScope 读回；React 已把已采纳 AI 日程草稿通过 `acceptedFromArtifactId` 关联到今日 `coach-event-*` 任务完成状态，能在 AI 反馈复盘和 Coach 页头部显示本地日程级采纳后完成率；React 复盘页已能用最近 7 日证据生成闭环分、有效信号、风险和下周焦点，也可读取和保存服务端周结果归因快照；Node/Rust `/api/coach/feedback` GET 已返回采纳率、拒绝类型、拒绝原因和下一轮提示校准 `summary`；Node/Rust `/api/coach/outcomes` GET/POST 已按 runtime 计算有效推进、采纳日程完成率、面试复盘率、延期和证据分布，并写入 `coachOutcomeSnapshots`；Rust 已把每次草稿生成写入独立 `llm_runs` 表，把采纳/拒绝写入独立 `llm_feedback` 表，并提供 `/api/coach/llm-runs`、`/api/coach/feedback` 读回；边界候选提取已能识别跨角色通用技术主题并过滤已保存主题，Node/Rust 已有 Anthropic-compatible provider 路径和 mock provider 合同测试，用户采纳后才写入正式知识边界，且正式边界保留来源摘要、置信度、provider、prompt version 和输入 hash；React 已记录边界候选采纳/拒绝/修订反馈，并进入 runtime sync、More 导出/导入和校准摘要，同时 best-effort 提交 `/api/coach/boundary-feedback`；Node fallback 保存到 `progress.coachBoundaryFeedback` 与 `progress.coach.boundarySuggestionFeedback`，Rust 保存到 `coach_boundary_feedback` 表，GET 返回 `topTopics`、`recentReasons` 和 `nextExtractionHint` summary；新增/补录实体 ID 已避免同毫秒覆盖上一条 | `PASS_WITH_LIMITS`，Node/Rust 均有 Anthropic-compatible 接口、feedback summary、onboarding-events summary、onboarding-report 聚合、invitations 台账、boundary-feedback 反馈账本、outcomes 周结果快照、规则版 JD 解析、批量邀请导入、users file 账号生命周期、批次状态更新、模板版本、邀请记录删除和通知草稿本地合同，远端账号生命周期、通知草稿和 UI 登录切换 smoke 已通过，Rust 已有本地 mock provider 合同证据，boundary suggestions 已有 provider/fallback 合同证据，React 已覆盖采纳后边界 provenance、候选采纳/拒绝/修订反馈、结果指标展示和 Review 页服务端周结果归因入口；远端真实 DeepSeek LLM evidence 已通过；真实 LLM 深度 JD parser、批量初始化审计、外部 SMTP/IM 自动发送、完整岗位题库匹配、真实 LLM 周复盘和机会/面试结果长期归因未做 |
| 不同用户拥有自己的日程、知识边界和 AI 草稿 | `node tests/auth_permissions_test.js` 与 `cargo test --manifest-path apps/rust-api/Cargo.toml runtime_contract_matches_node_core_api` 已覆盖 `kai` 与 `alex` 两个可写账号，分别写入和读取 coach runtime，交叉断言不包含对方画像、日程和草稿，并查询 Rust SQLite `runtime_items` 原始行；React 工作台已显示当前账号、角色、数据域和只读/可编辑状态，并提供登录/退出入口；`apps/react-web/src/test/sprintStoreCoachIsolation.test.ts` 和 `scheduleAdapter.test.ts` 已覆盖本地多画像切换后今日任务只显示当前画像的自定义日程 | `PASS_WITH_LIMITS`，Node/Rust 服务端隔离、React 多画像日程隔离和 UI 边界可见性已证实，公开注册和组织租户未做 |
| 总结可复用工作流 | `docs/product/product-ops/` 已新增长期文档规则、产品账本、已知问题和迭代工作流 | `PASS` |

## 不能扩大解释的范围

1. 当前 AI coach artifact 和 boundary suggestions 均已有服务端接口，已证明 Node provider 接口、Node/Rust fallback 合同、Rust 本地 mock provider 成功链路、规则版结构化 JD 解析，以及 boundary suggestions 的 Node/Rust mock provider 成功与超时 fallback；`docs/evidence/server-remote/coach-artifacts.json` 已证明远端 DeepSeek provider 调用、schema、token/延迟/成本字段、feedback summary 和 outcomes 读写通过。但这只证明真实模型链路可用，不等于真实 LLM 深度 JD parser、真实 LLM 周复盘或长期结果归因质量已经产品化。
2. 当前多用户只证明 Node/Rust 服务端账号作用域隔离，不是公开注册或组织多租户。
3. 当前服务器 HTTP 页面可访问，且登录、session、health、React 入口、`/api/progress` 写入和 marker 读回已通过；这证明 HTTP 远端服务端链路可用，但不等于 HTTPS/Android 远端生产交付完成。
4. 当前 `/api/coach/boundary-suggestions` 证明的是 provider/fallback 候选提取、人工确认流、采纳后 provenance 和 React 本地候选采纳/拒绝/修订反馈；当前 `/api/coach/boundary-feedback` 证明的是 Node/Rust 服务端边界候选反馈账本、summary 和 Rust 独立表落库；当前 `/api/coach/outcomes` 证明的是服务端按周读取 runtime 结果、计算有效推进/采纳后完成/面试复盘并保存快照，不等于真实 LLM 周复盘或机会/面试结果长期归因；当前快速初始化证明的是本地最小向导可跑；当前 `/api/coach/invitations` 已通过本地批量导入合同和远端 SQLite 新增/读回 smoke，users file 开通/重置/禁用/恢复/删除和邀请通知草稿已通过 Node/Rust 合同测试和远端账号生命周期 smoke；当前规则版 JD 解析证明的是本地结构化 fallback，不等于外部 SMTP/IM 自动发送、远端真实 LLM 深度 JD parser、批量初始化审计或批量导入知识边界。
5. 当前团队工作是 `TEAM_ROOM_PARTIAL`，不是 `TEAM_ROOM_PASS`。

## 2026-07-07 续跑验证补充

本次续跑发现一个自动化验收脆弱点：教练页新增邀请账号管理和快速初始化后，同页出现 `邀请角色族`、`快速角色族`、`角色族` 以及多个 `目标岗位` 字段，原本全页面 `getByLabel("角色族")` 和 `getByLabel("目标岗位")` 会在 Playwright 严格模式下命中多个控件。已把 Web、Rust/SQLite 和 Android WebView 三条端到端测试的主画像表单操作限定到 `#coach-profile` 容器，避免以后同页新增表单污染核心画像流程验收。

续跑结果：

- `npm run test:local-functional`：`PASS`。React Web 流完成学习笔记、口述、延期、资料/机会、复盘、画像、知识边界、自定义日程、AI 草稿接受/编辑/拒绝、浏览器重启读回、移动视口读回和导入恢复；Rust/SQLite 流完成同类业务状态写入，`profileCount=1`、`boundaryCount=2`、`scheduleEventCount=2`、`aiArtifactCount=3`、`llmRunCount=1`。
- `npm --prefix apps/react-web run build`、`npm run sync:android-react`、`gradle -p apps/android :app:assembleDebug`、`adb install -r apps/android/app/build/outputs/apk/debug/app-debug.apk`：`PASS`。
- `npm run test:android:functional`：`PASS`。本地 WebView 使用 `file:///android_asset/react/index.html`，App 重启后仍读回学习笔记、口述、延期、复盘、机会反馈、画像、知识边界、两条日程、三条 AI 草稿和一条 `local-fallback` LLM 运行记录。
- `npm test`：`PASS`。React typecheck 通过，Vitest 21 个测试文件、72 个用例通过，敏感扫描未发现高风险命中。
- `npm run validate:delivery -- --allow-dirty`：`FAIL`。失败仍集中在 Android 远端 HTTPS evidence 缺失、`JOB_SPRINT_ANDROID_WEBVIEW_URL` 当前为 HTTP、final delivery report 非 PASS；这不影响本地 Web/Android/Rust 功能结论，但阻塞完整远端交付结论。
- HTTPS 复测：`https://job-sprint.example.com/job-sprint/react/index.html` 可完成 TLS 并返回 302，HTTP 域名可 308 到 HTTPS，但 `https://job-sprint.example.com/job-sprint/react/index.html` 在域名 SNI ClientHello 后 reset；服务器侧 tcpdump 看到 ClientHello 到达后收到客户端方向 RST。IP HTTPS 不能作为 Android 远端验收替代，因为证书不匹配，Android WebView 已按安全要求取消 SSL 错误。
- 防假通过补强：`tests/android_webview_functional_persistence_test.js` 和 `tools/validate_final_delivery_readiness.js` 已拒绝 remote 模式下页面、flow/restart 快照或 auth session 状态落到 `file:///android_asset/...` 的报告，防止公网 HTTPS 失败后本地 fallback 被误判为 Android 远端通过。

## 后续完成条件

要从 `PASS_WITH_LIMITS` 升级为完整完成，需要补齐：

1. 用户明确允许在新任务中重新启用 subagents，且 runtime preflight 不再命中 quarantine 后，再做一次真实 Team Room 审计。
2. 在已补的 `/api/coach/outcomes` 周结果快照和远端 DeepSeek provider evidence 基础上，把真实 LLM 周复盘、机会状态和面试结果长期归因串起来；`llm_runs` 已具备 model、token、延迟和可选成本字段，`llm_feedback` 已具备服务端 summary，React 已具备本地日程级归因和本地 7 日周复盘归因，React/Node/Rust 已具备规则版 JD 解析，Node/Rust 已具备服务端周结果快照，当前还缺真实 LLM 深度 JD parser、真实 LLM 周复盘和机会/面试结果长期归因。
3. 把当前邀请制首登编排、邀请台账、批量邀请导入、users file 账号生命周期、批量账号动作和可复制邀请通知草稿升级成正式账号首登系统，并补外部 SMTP/IM 自动发送和管理员初始化入口。
4. 把边界候选提取接入远端真实 LLM provider evidence，并把服务端边界反馈账本用于真实 LLM 质量归因，同时持久化真实 LLM 深度解释和批量初始化审计；当前已有 Node/Rust provider/fallback 路径、本地 mock 合同、采纳后 provenance、React 候选采纳/拒绝/修订反馈、Node/Rust 服务端边界反馈账本和远端规则 fallback smoke。
5. 做邀请制账号初始化和用户管理后台；当前已补齐 UI 级当前账号、数据域、登录/退出入口、最小邀请台账、批量邀请导入、批次动作、导出 JSON、模板版本、邀请记录删除、users file 账号开通/重置/禁用/恢复/删除、批量账号禁用/恢复/删除、可复制邀请通知草稿、远端账号生命周期 smoke 和远端 UI 登录切换 smoke，但还缺外部 SMTP/IM 自动发送和更完整的用户管理后台。
6. 修复公网 HTTPS 443 外部握手问题，并把 Android remote URL 切到 `https://job-sprint.example.com/job-sprint/react/index.html`。
7. HTTPS 入口就绪后完成 Android 远端真机 evidence，并让 `npm run final:delivery -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/final-delivery/final-delivery.json` 写出 `PASS` 报告；`npm run validate:delivery` 才能从当前 `FAIL` 升级。

## 最新证据索引

- 远端 boundary suggestions smoke：`docs/evidence/server-remote/coach-boundary-suggestions-2026-07-07.json`，状态 `PASS`，返回 `MQ`、`RAG`、`稳定性` 并过滤已有 `Redis`；该报告生成于 DeepSeek provider 配置前，provider 为 `local-fallback`，后续若要证明边界候选的真实 LLM 质量，需要按同口径重跑 boundary suggestions 远端 provider evidence。
- React 邀请制首登、快速初始化和邀请账号管理：`npm --prefix apps/react-web test -- --run src/test/coachFirstLoginFlowAdapter.test.ts src/test/coachOnboardingMaterialAdapter.test.ts src/test/CoachPage.test.tsx`，状态 `PASS`，覆盖账号/数据域、首登画像模板、批量素材包、三条初始化边界、首条个人日程和首条 AI 草稿确认的 1/5 到 5/5 首登编排；覆盖首登完成率 20% 到 100%、当前放弃点、风险等级和下一步动作；覆盖邀请账号管理面板和本地降级提示；覆盖套用测试角色族首登模板、追加面试反馈素材、保存画像、生成初始化边界候选、采纳三条边界、生成首条日程和准备度推进；`coachAdapter.test.ts` 覆盖 0/4 到 4/4 闭环推进，并覆盖连续新增知识边界不会覆盖上一条。
- React Coach 结果指标：`npm --prefix apps/react-web test -- src/test/coachAdapter.test.ts src/test/CoachPage.test.tsx`，状态 `PASS`；`CoachDashboard` 会根据已完成且有验证证据的任务计算 `本周有效推进`，复用 AI 草稿采纳日程完成率作为 `采纳后完成`，并按带候选题任务的面试/口述/复盘证据计算 `面试复盘`。
- 服务端周结果归因：`node tests/node_ai_routes_boundary_test.js && node tests/api_runtime_test.js && node tests/rust_ai_routes_boundary_test.js && cargo test --manifest-path apps/rust-api/Cargo.toml runtime_contract_matches_node_core_api --test runtime_contract`，状态 `PASS`；覆盖 Node/Rust `/api/coach/outcomes` GET/POST 权限、周归因计算、`coachOutcomeSnapshots` 快照写入和 React Review 页服务端周结果归因入口；`npm --prefix apps/react-web run typecheck && npm --prefix apps/react-web test -- src/test/ReviewPage.test.tsx` 状态 `PASS`。
- 邀请账号服务端合同：`node tests/auth_permissions_test.js` 覆盖 Node owner 写入/读回、非 owner 403、邀请摘要和 JSON 持久化；`node tests/invitation_account_provisioning_test.js` 覆盖 users file 账号开通、密码重置、旧密码失效、新用户登录、单账号禁用/恢复/删除、批量账号禁用/恢复/删除、禁用后拒绝登录、恢复后可登录、删除后拒绝登录、响应不含密码/hash 和 runtime 隔离；`cargo test --manifest-path apps/rust-api/Cargo.toml runtime_contract_matches_node_core_api` 覆盖 Rust `/api/coach/invitations`、SQLite `coach_invitations` 落库、非 owner 403、users file 账号开通/重置/禁用/恢复/删除、批量账号禁用/恢复/删除和新用户登录隔离。
- 本地 boundary suggestions provider/fallback：`node tests/api_runtime_test.js` 覆盖 Node mock provider 成功和 provider timeout fallback；`cargo test --manifest-path apps/rust-api/Cargo.toml boundary_suggestions_use_provider_and_filter_existing_topics` 覆盖 Rust mock provider 成功并过滤已有 `Redis`。
- React 边界候选 provenance：`npm --prefix apps/react-web test -- --run src/test/boundarySuggestionAdapter.test.ts src/test/coachBoundaryProvenance.test.ts src/test/coachAdapter.test.ts src/test/CoachPage.test.tsx`，状态 `PASS`，覆盖候选边界带来源摘要、置信度、provider、prompt version 和输入 hash，并在采纳后写入正式知识边界。
- 服务器同步 evidence：`docs/evidence/server-sync/sync.json`，manifest SHA-256 `e5a1d8195791d988b5e50a0c9badc310771d303dfe34a63167629f5919ae53f2`；`docs/evidence/server-remote/service-restart.json` 证明 `job-sprint.service` 已重启且读取同一远端 manifest；远端 coach artifact smoke 已返回 DeepSeek 生成物、feedback 列表和 summary。
- 远端 coach artifact / DeepSeek smoke：`docs/evidence/server-remote/provider-config.json` 状态 `PASS`，证明远端 systemd drop-in 已配置 provider 并重启服务；`docs/evidence/server-remote/coach-artifacts.json` 状态 `PASS`，远端 provider 为 `anthropic-compatible`、model 为 `deepseek-v4-flash`，`llmRunStatus=success`、`llmRunSchemaStatus=pass`、`providerSemanticChecks` 全部为 true，且 `/api/coach/outcomes` GET/POST、feedback summary 和 `llm_runs` 读回均通过。
- 远端 React 与邀请账号 smoke：`docs/evidence/server-remote/coach-invitations.json` 和 `docs/evidence/server-remote/coach-invitations-2026-07-07.json`，状态 `PASS`；登录态访问 `/job-sprint/react/index.html` 为 200，React JS 资产包含 `/api/coach/invitations` marker，`/api/coach/invitations` 在远端 SQLite 中新增并读回 `codex-smoke-*` 记录，读回 `templateVersion=jd-focus-v1`；本轮脚本已扩展 `invitations_bulk_import`，用于证明远端批量导入两条 smoke 邀请记录；smoke 后已批量更新测试批次为 `paused`，随后删除 smoke 邀请记录，避免误当成待邀请用户。
- 远端 users file 账号生命周期 smoke：`docs/evidence/server-remote/coach-invitation-account.json`，状态 `PASS`；远端 `/api/coach/invitations` 返回 `accountProvisioning.enabled=true`，`codex-smoke-account` 开通后可登录，`/api/auth/session` 读回 username/dataScope，smoke 用户写入 runtime 后 owner runtime 不包含该 marker，且响应未泄露生成密码或 sha256 hash；随后已验证单账号禁用后登录 401、恢复后登录 200、删除后登录 401，并验证批量禁用两个账号后登录均 401、批量恢复后均可登录、批量删除后被删除账号登录 401；本轮还验证 `invitation_notifications_generated`，按邮件/IM/手工渠道生成 2 条可复制邀请通知草稿且不泄露密码/hash。
- 远端 UI 登录切换 smoke：`docs/evidence/server-remote/login-switch.json`，状态 `PASS`；Playwright 打开远端真实登录页，完成 owner UI 登录、点击页面“退出”、回到登录页、切换 `codex-ui-switch-smoke` 用户登录、读回 `username` 和 `dataScope`，并在最后删除临时账号后验证登录 401；截图在 `docs/evidence/server-remote/login-switch-screenshots/`。
- Linux x86_64 Rust ELF SHA-256：`a785bef602c15959f77bdc08957e1de378d132d20ce55dc6a1d9ca5e21d0a89f`。
- 正式 APK evidence：`docs/evidence/android-release/formal-release.json`，APK SHA-256 `221c7483d288c5642304d51786fe6f8a664046c2079d3f8ad2df1dcc281d8343`。
- 最终 readiness：`npm run validate:delivery -- --allow-dirty` 仍为 `FAIL`，失败项为 Android remote HTTPS evidence、final delivery report 和由此引发的 goal acceptance。
