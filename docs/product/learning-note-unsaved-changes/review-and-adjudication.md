# 评审与裁决

## 产品评审

- 学习笔记不是普通筛选输入，而是后续面试练习和 Evidence Gate 的来源；丢失会打断闭环。
- 空草稿取消不确认，避免每次打开后退出都出现无意义弹窗。

## 技术评审

- 项目已有 `useRouteLeaveGuard`，并已经用于画像、日程、知识边界和复盘草稿。
- 仅在 `noteTaskId` 存在且文本去除空白后非空时注册守卫，避免把已保存或未输入状态误判为脏数据。

## 裁决

采用局部学习笔记取消确认，并接入既有路由离开守卫；不增加持久化草稿或任何服务端写入。
