# 未保存内容路由离开保护开发与验证流程

日期：2026-08-01

1. 审计路由实现、导航入口与现有草稿基线，确认站内链接可由 AppShell 统一捕获。
2. 建立 feature capsule，记录 HashRouter 与系统返回边界。
3. 新增路由离开 Provider 与通用注册 Hook；Provider 同时管理站内链接确认和 `beforeunload`。
4. 将画像、知识边界、日程、机会、复盘的既有 dirty 判断接入 Hook，不改变数据写入和局部确认语义。
5. 增加 Provider 行为测试，并跑既有页面草稿保护回归。
6. 运行类型检查、全量 React 测试、构建、架构门禁、根测试与敏感扫描。
7. 更新产品账本、已知问题和日更日志，走 PR 合入 `develop`；达到版本阈值后按 GitFlow 发布。
