# 开发与验证流程

1. 为面试 adapter 增加画像与机会记录的轻量候选题上下文。
2. 按五种题型生成不同候选题，并保留自动题型的当前任务来源。
3. 面试页从当前用户状态和机会 adapter 传入上下文，保持 Evidence Gate 任务归属不变。
4. 新增 adapter 与页面回归，覆盖题型差异、已记录 JD、无 JD 降级和草稿替换确认。
5. 运行 Web、Android、功能、架构、安全与 GitFlow 门禁。
