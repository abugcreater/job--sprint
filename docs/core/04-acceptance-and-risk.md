# 验收与风险

日期：2026-07-10

## 2026-07-11 HTTPS 生产交付更新

备案与正式证书生效后，公网 HTTPS 阻塞已解除。仓库外私有 env 注入的正式域名已通过 HTTP 308、HTTPS 登录/session、`/api/progress` 写入读回、最新 React/Rust 部署与服务重启；正式 APK 已关闭 cleartext、注入同一 HTTPS React URL并通过 v2/v3 验签，OnePlus 8 Pro 已完成远端 HTTPS 登录、完整业务流和杀进程重启读回。最终 runner 为 `PASS_WITH_LIMITS`，限制来自未提交工作树和 P8 理论性架构门禁，不是 HTTPS 或生产数据链路缺口。完整证据见 `docs/product/product-flow-redesign/p5-https-production-delivery.md`。下文 2026-07-06/07 的 `PUBLIC_443`、HTTP 和占位域名结论只保留为历史诊断记录，不得覆盖本节最新事实。

## 推荐验证命令

| 场景 | 命令 | 说明 |
|---|---|---|
| 常规只读门禁 | `npm test` | 语法、单测、路径、工作树边界、敏感扫描；不构建 public-safe。 |
| 发布前门禁 | `npm run test:release` | 在 `npm test` 后执行 `npm run test:local-functional`，显式覆盖 Web 功能持久化和 Rust/SQLite UI 入库，再运行 `npm run build:rust:linux`、构建 public-safe、扫描 dist / Android fallback，并生成 server delivery package；server delivery package 会拒绝 macOS Mach-O，必须提供 Linux x86_64 ELF。 |
| React 类型检查 | `cd apps/react-web && npm run typecheck` | TypeScript app / node config。 |
| React 单测 | `cd apps/react-web && npm test` | Vitest。 |
| Web 保存进度 | `npm run test:functional` | Playwright 真实浏览器，可能产生测试证据文件。 |
| Rust/SQLite UI 入库 | `npm run test:rust:functional` | 启动真实 Rust/Axum 服务，React UI 点击关键功能后直接查询 SQLite `runtime_items`。 |
| Rust 后端合同 | `cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check && cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings && cargo test --manifest-path apps/rust-api/Cargo.toml && cargo build --release --manifest-path apps/rust-api/Cargo.toml` | 验证 SQLite runtime、legacy JSON 迁移、用户同步、Bearer token 写入 SQLite 和 release 构建。 |
| 架构质量门禁 | `npm run validate:architecture-quality` | 检查 Rust/Android/Node 拆分模块仍存在、关键入口文件保持薄层、旧 `assets/schedule.js` 与 Android fallback 拷贝 hash 一致且不继续膨胀；已接入 `npm test`。 |
| 功能覆盖门禁 | `npm run validate:functional-coverage` | 检查 Web、Android WebView、Rust/SQLite UI 三条功能测试链仍覆盖今日、延期、学习、面试、投递、复盘、更多页、重启读回和 SQLite 入库证据；已接入 `npm test` 与 `npm run validate:delivery`。 |
| 功能对齐门禁 | `npm run validate:feature-parity` | 按 10 项矩阵校验 Web 与 Android 本地功能覆盖：登录/session 能力入口、今日证据、延期、学习、面试、投递、复盘、更多页导入、AI 教练和重启读回；该门禁已作为 `feature_parity` 直接接入 `npm run validate:delivery`，但只证明本地/能力对齐，不替代 Android 远端 HTTPS 真机 evidence。 |
| 产品迭代工作流门禁 | `npm run validate:product-iteration` | 校验多版本 PRD、推荐 PRD、团队审计降级口径、可复用开发工作流、UI 防回归规则、AI 草稿契约、已知外部限制和远端 coach artifact evidence；该门禁已接入 `npm test` 与 `npm run validate:delivery`，防止下次产品迭代只改功能不更新 PRD/复盘/门禁，也防止最终交付报告绕过产品闭环检查。 |
| 目标验收门禁 | `npm run validate:goal-acceptance -- --allow-user-action` | 按原始 7 项目标逐项汇总 Web/Android 对齐、DB 入库、前后端 Rust 边界、架构质量、重构后测试、服务器同步和正式 APK 证据；缺远端 URL、SSH、Android 远端真机 evidence 或最终报告时返回 `USER_ACTION_REQUIRED`，不允许把局部 PASS 冒充整体完成；已接入 `npm test` 与 `npm run validate:delivery`。 |
| 交付 env 初始化/补齐 | `npm run init:delivery-env -- --merge-missing` | 对仓库外私有 `~/.job-sprint/job-sprint-delivery.env` 只追加缺失变量名、可选部署 SSH key/port 槽位和默认最终报告路径，保留已有正式签名材料、账号和密码值；适合在已经生成 Android release keystore 后补齐服务器、远端 URL、Android 真机验收等空槽位。 |
| 交付外部输入预检 | `npm run validate:delivery-inputs -- --delivery-env-file /path/outside-repo/job-sprint-delivery.env --allow-user-action` | 只读检查服务器同步、远端验收、Android 远端真机、正式签名和最终报告所需 env/tool 是否就绪；`--delivery-env-file` 必须指向仓库外私有 `KEY=VALUE` 文件，仓库内 env 文件会被拒绝；输出只包含 configured/missing 布尔、变量名和 env 文件加载状态，不输出密码明文；该预检不代表已同步服务器、已跑真机或已打正式包。 |
| 服务器同步包 | `npm run build:server-delivery` | 将 `apps/rust-api/target/release/job-sprint-api` 和 `dist/public-safe` 复制到 `dist/server-delivery/`，生成 `server-delivery-manifest.json`；同步包生成前必须先准备 Linux x86_64 ELF，当前 macOS 本机 `cargo build --release` 产物会被拒绝；`validate:delivery` 会校验同步包与当前 Rust binary、public-safe dist 一致，并拒绝 Rust 源码/Cargo/migration 文件比 release binary 更新的旧产物。 |
| 服务器同步 evidence | `npm run write:server-sync-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-sync/sync.json` | 通过 rsync/ssh 同步 `dist/server-delivery/`，并验证远端 `server-delivery-manifest.json` SHA-256 与本地一致；同步必须保护远端顶层 `shared/`，防止 `rsync --delete` 删除 SQLite runtime、上传或其他运行时数据；支持从仓库外私有 `--delivery-env-file` 读取服务器 SSH 目标、`JOB_SPRINT_DEPLOY_SSH_KEY`、`JOB_SPRINT_DEPLOY_PORT`，也支持 CLI `--identity-file`/`--port`；报告只显示 env 文件加载状态、变量名和 key 是否配置，不输出 key 内容；缺 SSH 目标或 evidence 时 `validate:delivery` 保持 `USER_ACTION_REQUIRED`。 |
| 远端服务重启 evidence | `npm run restart:remote-service -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/service-restart.json` | 同步 Rust binary 或静态资源后重启 `job-sprint.service`，并读回远端 `server-delivery-manifest.json` SHA-256；报告只显示服务、host、manifest hash 和 key 是否配置，不输出 SSH key 或账号密码；该项防止“rsync 成功但旧进程继续响应”的误判。 |
| 服务器远端验收 evidence | `npm run write:remote-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/acceptance.json` | 运行远端 Web 登录、session、health、静态入口和 `/api/progress` 保存读回检查，生成脱敏 JSON；支持从仓库外私有 `--delivery-env-file` 读取远端 URL 和认证账号；`validate:delivery` 会校验登录、session、health、progress remote save 和 progress remote readback 输出存在。 |
| 本地邀请账号开通合同 | `node tests/invitation_account_provisioning_test.js && cargo test --manifest-path apps/rust-api/Cargo.toml runtime_contract_matches_node_core_api` | 使用仓库外临时 `JOB_SPRINT_USERS_FILE` 验证 owner 管理员可从 `/api/coach/invitations` 开通或重置普通账号，不能通过邀请开通 owner 权限；新用户可登录，旧密码失效，禁用/恢复/删除与批量账号动作会写入 `accountAuditEvents` 审计事件，响应不含密码/hash，Node/Rust runtime 均保持数据隔离。 |
| 远端邀请台账 evidence | `npm run write:remote-invitation-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/coach-invitations.json` | 运行登录态 React 入口、React JS marker、`/api/coach/invitations` 新增/读回、批量导入、批次暂停和 smoke 记录删除；用于验证邀请账号管理和 React 新入口真的部署到远端。该项只证明远端邀请台账可用；真实登录账号开通必须另跑远端邀请账号开通 evidence。 |
| 远端邀请账号生命周期 evidence | `npm run write:remote-invitation-account-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/coach-invitation-account.json --allow-create-account` | 显式允许后才会改远端 users file；验证远端 `accountProvisioning.enabled=true`、owner 可开通 smoke 账号、响应不泄露密码/hash、smoke 用户可登录并读回 session、写入 runtime 后 owner runtime 不串线，并覆盖禁用后拒绝登录、恢复后可登录、删除后拒绝登录、批量禁用/恢复/删除和 `invitation_notifications_generated` 邀请通知草稿。该项只证明邀请制 users-file 登录账号生命周期和可复制通知草稿，不代表公开注册、外部 SMTP/IM 自动发送、批量用户后台或 Android HTTPS 交付。 |
| 远端 UI 登录切换 evidence | `npm run write:remote-login-switch-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/login-switch.json --allow-create-account` | 使用 Playwright 打开远端真实登录页，临时开通 `codex-ui-switch-smoke`，验证 owner UI 登录、点击页面“退出”、回到登录页、切换 smoke 用户登录、session/dataScope 读回和账号清理；报告不输出生成密码或 hash。该项证明 Web 端真实登录切换链路，不代表公开注册、Android HTTPS 或外部 SMTP/IM 自动发送。 |
| Android 保存进度 | `npm run test:android:functional` | 需要 Android 真机或等价设备；脚本会唤醒设备并在 USB 连接期间保持亮屏，退出时恢复。 |
| Android 远端登录与保存进度 | `npm run test:android:remote:functional -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env` | 复用真机点击流程生成 `docs/evidence/android-remote-functional/android-webview-functional-persistence-report.json`；远端 WebView URL 必须是 HTTPS，且路径必须包含 `/job-sprint/`，HTTP 公网 IP 只能用于服务器 HTTP 验收，不能替代 App 远端验收；支持从仓库外私有 `--delivery-env-file` 读取远端 WebView URL 和认证账号；报告必须包含 `authEvidence`、账号密码配置状态、登录页/登录尝试记录、`/api/auth/session` authenticated 状态和保存后重启读回快照，用于 `validate:delivery` 的 Android 远端 evidence。 |
| Android release signing 初始化 | `npm run init:android-release-signing -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --write-env` | 在没有既有长期 release keystore 时生成仓库外私有 keystore，读取证书 SHA-256，并把签名变量写入仓库外私有 env；默认不覆盖已有 signing env 或已有 keystore，报告不打印密码，生成后仍必须运行正式 APK 构建和 `validate:delivery` 验签。 |
| 最终交付编排 | `npm run final:delivery -- --delivery-env-file /path/outside-repo/job-sprint-delivery.env --report docs/evidence/final-delivery/final-delivery.json` 或 `npm run final:delivery -- --dry-run --delivery-env-file /path/outside-repo/job-sprint-delivery.env --report /tmp/job-sprint-final-delivery-dry-run.json` | 先做工具/env preflight，并在 preflight 报告里嵌入与 `npm run validate:delivery-inputs` 同源的 `externalInputs` 结构化缺口，再串联 release gate、服务器同步 evidence、服务器远端验收、Android 远端验收、正式 APK 构建和最终 readiness；`--delivery-env-file` 必须指向仓库外私有文件，报告只显示加载状态和变量名；`--dry-run` 只输出计划、工具可用性和缺少的输入，不触发 rsync、真机或正式签名构建；`--report`/`JOB_SPRINT_FINAL_DELIVERY_REPORT` 用于留存统一报告；runner 内部最终 readiness 会延后自报告校验，避免报告写入前自引用；初步 PASS 报告写入后会追加 `post_final_report_validation`，该阶段只允许自身步骤尚未写入；最终落盘报告缺少 `post_final_report_validation=PASS` 时不能通过。 |
| 最终交付 readiness | `npm run validate:delivery` | 只读汇总 release gate 脚本防回退、架构质量门禁、功能覆盖门禁、功能对齐门禁、产品迭代工作流门禁、交付外部输入预检、Linux x86_64 ELF 目标平台校验、public-safe 待发布包扫描、server delivery package、服务器同步 evidence、最终交付统一报告、Web/服务器远端 evidence、远端 UI 登录切换 evidence 工具存在性、Android 远端 HTTPS 首访/登录/session/保存/重启读回证据、Rust release binary freshness、正式 Android 签名输入、APK SHA-256 和 FORMAL_SIGNED 报告；缺输入时返回 `USER_ACTION_REQUIRED` 或 `PASS_WITH_LIMITS`；只有 runner 内部同时带 `JOB_SPRINT_FINAL_DELIVERY_IN_PROGRESS=1` 和 `--defer-final-delivery-report` 时才允许最终报告项延后。 |
| Android 正式 APK | `npm run build:android:release -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/android-release/formal-release.json` | 严格要求仓库外 release keystore、alias、密码和预期证书 SHA-256；支持从仓库外私有 `--delivery-env-file` 读取签名材料；构建工具会拒绝仓库内 keystore，并写出包含 `apkSha256` 的验签 JSON，`validate:delivery` 会重新计算 APK SHA-256，只接受 `FORMAL_SIGNED`、证书 SHA-256 匹配且 APK SHA-256 匹配的报告。缺失时必须标 `USER_ACTION_REQUIRED`。 |
| Android APK 验签 | `npm run verify:android:release -- --apk <apk>` | 用 `apksigner verify --print-certs` 验证已有 APK；未 pin 证书只能算 `SIGNED_UNPINNED`。 |
| 工作树边界 | `npm run validate:workspace-boundaries` | 验证项目内没有全局 AI 团队影子入口回流。 |
| Codex AI 团队 preflight | Codex 对话层只读检查 | 可选验证全局团队触发协议；不作为 Job Sprint release gate。 |

## 当前验收结论

2026-07-08 真实用户复验收口：以“林启航，6 年测试平台和接口自动化经验，目标测试开发工程师 / QA Automation Engineer，杭州/上海，28-35K”为测试用户，使用真浏览器完成新用户空态、导入简历建档、确认求职画像、采纳知识边界、生成今日行动、补证据、标记完成、统计查看、今日复盘和更多页普通用户视图。结果写入 `/tmp/job-sprint-flow-result.json`，20/20 检查通过，体验评分 88 分；截图留存于 `/tmp/job-sprint-empty-learn.png`、`/tmp/job-sprint-empty-interview.png`、`/tmp/job-sprint-flow-coach.png`、`/tmp/job-sprint-flow-today.png`、`/tmp/job-sprint-flow-stats.png`、`/tmp/job-sprint-flow-review.png`、`/tmp/job-sprint-flow-more.png`。复验加严检查了空知识/面试不显示旧静态 Java 内容，个人画像日程后的 Today/Review 不再出现 `Spring 事务`、`G1/ZGC`、`事务/MVCC/Redis`、`准备 09:30 的 Spring` 等默认日程残留；补证据并标记完成后仍保留“取消完成”确认态，复盘页缺证据计数归零。最新验证通过：`npm --prefix apps/react-web run typecheck`、12 个 React 目标测试文件 42 条用例、`npm --prefix apps/react-web run build`、`node tools/sync_react_android_assets.js`、`diff -qr apps/react-web/dist apps/android/app/src/main/assets/react`。该结论证明 Web 与 Android 内置 React 资产已同步到本轮产品闭环修复；仍不替代 Android 远端 HTTPS 真机验收或正式 SaaS 发布验收。

2026-07-08 产品闭环与权限修复：AI 团队采用 Manager Dispatch，产品评审和技术评审共同确认主线为“导入简历/JD/反馈 -> 确认求职画像 -> 生成知识边界和今日行动 -> 执行补证据 -> 复盘归因 -> 下一轮 AI 建议”。React 已把“快速初始化/目标画像/AI 草稿”统一为“导入简历建档/求职画像/AI 建议”，新增简历文本/Markdown 导入解析与确认写入，画像保存、删除、AI 建议接受/拒绝和复盘保存均有明确反馈；知识、面试新用户空态不再默认展示旧 Java 内容；复盘页调整为今日复盘、复盘建议、本周复盘和复盘历史；更多页普通用户只展示我的账号、同步、个人数据备份和常用入口，管理员邀请与批次能力仅在管理员中心。Web 产物已通过 `npm --prefix apps/react-web run build` 构建，并用 `node tools/sync_react_android_assets.js` 同步到 `apps/android/app/src/main/assets/react`，`diff -qr apps/react-web/dist apps/android/app/src/main/assets/react` 无差异。当前验证通过：`npm --prefix apps/react-web run typecheck`、10 个 React 目标测试文件 34 条用例、`node tests/auth_permissions_test.js`、`node tests/feature_parity_test.js`、`node tests/android_webview_initializer_test.js`、`node tests/android_remote_webview_controller_test.js`。该结论证明 Web 与 Android 内置 React 资产一致、Node server-json runtime 用户数据隔离有效、普通用户权限边界收紧；仍不等于 Android 远端 HTTPS 真机验收或正式公开 SaaS 完成。

2026-07-06 AI 求职教练迭代收口：产品定位已从个人高级 Java 求职冲刺工具升级为面向泛 IT 求职者的 AI 求职教练；React Web 和 Android 本地 WebView 已覆盖画像、知识边界、自定义日程、AI 草稿生成/编辑/接受/拒绝、AI 反馈复盘、本地日程级采纳后完成率归因、本地规则版复盘 AI 分析、本地 7 日周复盘归因、当前账号与数据域可见性、今日页联动、导入导出和重启读回。最新验证中 `npm test`、React typecheck、React Vitest、React build、Rust cargo test、Rust/SQLite UI 入库、Linux x86_64 Rust binary 构建、server-delivery、服务器同步、服务器 HTTP 远端验收和正式签名 APK 均通过；Android 远端验收仍被 HTTPS URL 前置条件阻塞。

2026-07-06 多用户隔离补证：`node tests/auth_permissions_test.js` 与 `cargo test --manifest-path apps/rust-api/Cargo.toml runtime_contract_matches_node_core_api` 已覆盖 `kai` 与 `alex` 两个可写账号分别写入 coach runtime，并交叉断言对方的画像、知识边界、日程和 AI 草稿不可见；Rust 测试还会查询 SQLite `runtime_items` 原始行，证明 `scope` 隔离落库有效。这证明服务端作用域隔离有效，但不等于公开注册或组织租户已完成。

2026-07-06 AI coach artifact 补证：新增 `/api/coach/artifacts`。Node 兼容服务和 Rust 正式服务均可在配置 `ANTHROPIC_BASE_URL` 和 `ANTHROPIC_AUTH_TOKEN` 后调用 Anthropic-compatible provider，失败时返回明确 fallback；Rust 合同测试已用本地 mock provider 覆盖 `generated-ai` 草稿、token、延迟和可选成本写入 `llm_runs` 并从 `/api/coach/llm-runs` 读回。React 生成 AI 草稿时会优先请求服务端，失败时提示本地规则 fallback。当前只证明接口、schema、权限、本地 provider mock 和 fallback，不证明远端真实大模型已生产可用。

2026-07-06/07 角色族 playbook 与题卡补证：React 本地规则、Node fallback、Rust fallback 和 provider prompt 均已接入 11 个泛 IT 角色族 playbook 和首批角色追问题卡，生成草稿时会带入角色视角、证据类型、回答框架、日程焦点、主候选题和追问库；`inputSummaryHash` 也纳入角色族，避免同一目标岗位但不同角色视角的 AI 运行记录混淆。当前证明的是角色族级个性化和首批追问库，不等于完整岗位题库、深度 JD 匹配或真实 LLM 线上质量。

2026-07-07 机会/JD 信号补证：React 已从投递/机会记录提取公司、岗位、状态、JD 关键词、命中点和反馈，生成 AI 草稿时传给服务端；React 本地规则、Node fallback、Rust fallback 和 provider prompt 均会把机会信号作为上下文，sources/reason 会引用当前机会，`inputSummaryHash` 会纳入机会信号；React/Node/Rust fallback 已进一步生成 `JD焦点` 和规则版 `JD解析`，并结合角色追问库让知识卡、日程建议和候选题围绕岗位责任、硬技能、风险信号、证据要求、主候选题和角色追问生成。远端 `docs/evidence/server-remote/coach-jd-focus-2026-07-07.json` 已证明服务端 sources、body 和 reason 包含 `JD焦点：MQ 的故障恢复`；本地 React/Node/Rust 专项测试已证明 `JD解析：硬技能 MQ、Redis、稳定性`、证据要求、解析题和追问库进入生成物；远端 `docs/evidence/server-remote/provider-config.json` 已证明 DeepSeek/Anthropic-compatible provider 写入 systemd drop-in 并重启服务，`docs/evidence/server-remote/coach-artifacts.json` 已证明 provider 为 `anthropic-compatible`、model 为 `deepseek-v4-flash`、`llmRunStatus=success`、`llmRunSchemaStatus=pass`，且生成物引用 `MQ`、`Redis`、`稳定性`、故障恢复和面试候选题语义。当前证明的是机会进展可以影响下一轮建议且真实模型链路已通，不等于真实 LLM 深度 JD parser、自动投递、企业 ATS 或长期机会结果归因已经产品化。

2026-07-07 AI 提取知识边界补证：React 教练页已新增“AI 提取边界”确认流，用户粘贴 JD、简历片段或面试反馈后，系统生成候选边界，用户点击采纳后才写入正式知识边界；采纳后的正式边界会保留来源摘要、置信度、provider、prompt version 和输入 hash；React 本地规则、Node `/api/coach/boundary-suggestions` 和 Rust `coach_boundary_suggestions` 均可识别跨角色通用技术主题，并过滤已保存主题；Node/Rust 服务端已补 Anthropic-compatible provider 路径，mock provider 成功和 provider timeout fallback 均有合同测试；`CoachPage.test.tsx` 覆盖画像保存、边界候选提取、采纳边界、provenance 显示、快速初始化、首条日程生成和后续 AI 草稿生成闭环，`coachBoundaryProvenance.test.ts` 覆盖采纳后 provenance 入库，`CoachBoundarySuggestionFeedbackPage.test.tsx` 与 `boundarySuggestionFeedbackAdapter.test.ts` 覆盖候选采纳、拒绝、修订反馈和校准摘要，Node/Rust 专项测试覆盖权限、schema 和 HTTP runtime。当前证明的是 provider/fallback 候选提取、人工确认、采纳后 provenance、React 本地/runtime/import-export 的候选采纳/拒绝/修订反馈，以及 Node/Rust `/api/coach/boundary-feedback` 服务端反馈账本和 summary；不等于远端真实 LLM 深度解析、批量导入知识边界、批量初始化审计或邀请制首登初始化。

2026-07-07 邀请账号生命周期补证：React 邀请账号管理已从纯台账升级为 users file 账号开通/重置/禁用/恢复/删除，并提供可复制邮件/IM/手工邀请通知草稿；Node `tests/invitation_account_provisioning_test.js` 和 Rust `runtime_contract_matches_node_core_api` 覆盖 owner 开通账号、密码 hash 写入、新用户登录、旧密码失效、禁用后拒绝登录、恢复后可登录、删除后拒绝登录、通知草稿生成、响应不含密码/hash、configured users 即时读回和 runtime 隔离。该能力只在服务端配置仓库外 `JOB_SPRINT_USERS_FILE` 且未使用 `JOB_SPRINT_USERS_JSON` 时启用；远端 `docs/evidence/server-remote/coach-invitation-account.json` 已进一步证明远端 `accountProvisioning.enabled=true`、`codex-smoke-account` 可登录、session 读回 username/dataScope、smoke runtime 写入不串 owner、禁用后登录 401、恢复后登录 200、删除后登录 401、`invitation_notifications_generated=2`，且响应未泄露生成密码或 sha256 hash。

2026-07-07 用户闭环与隔离修复：React 已把统计集中到 `/stats`，画像页只保留个人画像、知识边界、日程和 AI 草稿，邀请账号与批次首登移动到 owner-only `/admin`；普通用户更多页只展示同步、导出恢复、账号权限和常用入口。新用户没有画像时，知识和面试模块进入“先创建画像”空态，不再默认展示旧 Java 内容；画像保存和删除有明确反馈，删除会级联清理画像关联的知识边界、日程、AI 草稿和 LLM run。runtime sync 增加 `storageOwner`，账号 dataScope 不匹配时会先重置本地状态并阻止旧 localStorage 优先上传；纯 Vite dev 默认不再请求 `/api/runtime`，避免无后端预览产生 404 控制台错误。最新本地验收包括 React typecheck、15 个 React Vitest 文件 54 条测试、React build、Node 邀请账号合同、Rust runtime contract 和 Playwright 桌面/移动视觉 QA；Playwright 使用全新浏览器上下文验证移动空态、统计页、画像保存反馈和普通用户更多页权限隔离，截图路径为 `/tmp/job-sprint-mobile-empty.png`、`/tmp/job-sprint-stats-desktop.png`、`/tmp/job-sprint-coach-save.png`、`/tmp/job-sprint-more-ordinary.png`。

2026-07-07 远端 UI 登录切换补证：新增 `tools/write_remote_login_switch_evidence.js` 与 `npm run write:remote-login-switch-evidence`。脚本先用 owner API 临时开通 `codex-ui-switch-smoke`，再用 Playwright 打开远端真实登录页，完成 owner UI 登录、点击页面“退出”、回到登录页、切换 smoke 用户登录，并通过页面内 `/api/auth/session` 读回 `username=codex-ui-switch-smoke`、`dataScope=codex-ui-switch-smoke`；最后用 owner API 删除临时账号并验证删除后登录 401。远端 `docs/evidence/server-remote/login-switch.json` 状态为 `PASS`，截图位于 `docs/evidence/server-remote/login-switch-screenshots/`，脚本不输出生成密码或 hash。

当前交付口径仍需分层：本地功能、HTTPS 生产链路、Git 提交/合并状态分别陈述。仓库外私有 env 注入的正式 HTTPS 入口已通过；最终 runner 为 `PASS_WITH_LIMITS`，限制来自 `--allow-dirty` 与 P8 理论性架构门禁。2026-07-06/07 的公网 443 reset 结论属于备案完成前的历史诊断，不再代表现网状态。

| 能力 | 结论 | 边界 |
|---|---|---|
| 代码完成口径 | `PASS_WITH_LIMITS` | Web、Android、Rust/SQLite、本地常规门禁、public-safe、Linux ELF、server delivery、服务器同步、HTTPS 远端 evidence、正式签名 APK 和 Android 真机远端 evidence 已通过；限制为未提交工作树和 P8 理论性架构门禁。 |
| UI/UX 改造 | `PASS_WITH_LIMITS` | 全流程产品重构已通过 Web/Android 回归，并在正式 APK 上完成 HTTPS 远端真机验收；限制为单 chunk 告警和未提交状态。 |
| React Web 主流程 | `PASS` | `npm --prefix apps/react-web run typecheck`、`npm --prefix apps/react-web test -- --run src/test/runtimeClient.test.ts src/test/runtimeSyncBridge.test.ts src/test/CoachPage.test.tsx src/test/MorePage.test.tsx src/test/navigationRoutes.test.tsx src/test/LearningPage.test.tsx src/test/InterviewPage.test.tsx src/test/ApplicationsPage.test.tsx src/test/ReviewPage.test.tsx src/test/sprintStoreCoachIsolation.test.ts src/test/moreAdapter.test.ts src/test/applicationsAdapter.test.ts src/test/interviewAdapter.test.ts src/test/reviewAdapter.test.ts src/test/authClient.test.ts`、`npm --prefix apps/react-web run build` 通过；Playwright 已验证 `/stats`、画像保存反馈、普通用户更多页和移动端新用户空态。 |
| React 当前账号可见性 | `PASS` | React 工作台已补齐 `/api/auth/session` 读取、当前账号/角色/数据域/只读状态展示、未登录跳转登录和已登录退出入口；`apps/react-web/src/test/authClient.test.ts` 覆盖登录跳转、角色和数据域摘要。 |
| Android WebView 本地入口 | `PASS` | 已重新同步 React assets、构建并安装 debug APK，`npm run test:android:functional` 通过，报告显示 local WebView 画像、知识边界、日程和 AI 草稿状态可重启读回。 |
| Node runtime API | `PASS_WITH_LIMITS` | 兼容 `/api/runtime` 已通过；Node JSON 只作为兼容/回滚入口。 |
| Rust runtime API | `PASS_WITH_LIMITS` | 本地 SQLite 合同、legacy JSON 迁移、用户同步、Bearer token 写入 SQLite、release 构建、React UI 入库测试已通过；服务器已切到 Rust/SQLite release。 |
| Rust/SQLite UI 入库 | `PASS` | `npm run test:rust:functional` 已证明 React UI 点击和更多页导入恢复后，SQLite `runtime_items` 写入 `progress.delayRecords`、`reviews`、`applications`、`interview_mistakes`。 |
| 多用户 coach 数据隔离 | `PASS_WITH_LIMITS` | `node tests/auth_permissions_test.js` 证明 Node server-json runtime 按 `dataScope` 隔离 `progress.coach`；`cargo test --manifest-path apps/rust-api/Cargo.toml runtime_contract_matches_node_core_api` 证明 Rust/SQLite runtime 按 `scope` 隔离 `progress.coach` 并写入独立 `runtime_items` 行；React `runtimeSyncBridge.test.ts` 覆盖 localStorage `storageOwner` 与当前账号 dataScope 不匹配时禁止本地旧数据优先上传；公开注册和组织租户未完成。 |
| AI coach artifact API | `PASS_WITH_LIMITS` | `node tests/api_runtime_test.js` 覆盖 Node `/api/coach/artifacts` 未登录 401、schema、provider timeout fallback、prompt/schema/hash 元数据、角色视角来源、机会/JD 信号来源，以及 `/api/coach/feedback` 未登录 401、写入和读回；`cargo test --manifest-path apps/rust-api/Cargo.toml runtime_contract_matches_node_core_api` 覆盖 Rust `/api/coach/artifacts` 缺画像 400、正常草稿 schema、角色视角来源、机会/JD 信号来源、本地 mock provider 成功、`generated-ai` 草稿、token/延迟/可选成本写入、`/api/coach/llm-runs` 读回和 SQLite `llm_runs` 入库，并覆盖 `/api/coach/feedback` 写入/读取和 SQLite `llm_feedback` 入库；`docs/evidence/server-remote/provider-config.json` 已证明远端 DeepSeek/Anthropic-compatible provider 配置并重启服务，`docs/evidence/server-remote/coach-artifacts.json` 已证明远端 provider 为 `anthropic-compatible`、model 为 `deepseek-v4-flash`、`llmRunStatus=success`、`llmRunSchemaStatus=pass`、token/延迟/成本字段齐全、feedback summary 和 `/api/coach/outcomes` GET/POST 通过；React/Node/Rust 专项测试已证明规则版 `JD解析`、证据要求、解析题和角色追问库进入知识卡、日程建议、候选题、sources/reason/inputSummaryHash；React 已补用户态 AI 运行记录、AI 反馈复盘、采纳日程完成率归因、11 个角色族 playbook 与首批追问题卡、机会/JD 信号上下文、本地 7 日周复盘归因和接受/拒绝后的服务端反馈提交；完整岗位题库、真实 LLM 深度 JD 解析、服务端持久跨周归因和反馈聚合分析未完成。 |
| AI boundary suggestion API | `PASS_WITH_LIMITS` | React `CoachPage.test.tsx` 覆盖粘贴素材、提取候选、采纳边界、provenance 显示、快速初始化和后续 AI 草稿生成；`coachBoundaryProvenance.test.ts` 覆盖采纳后正式边界保留来源摘要、置信度、provider、prompt version 和输入 hash；`boundarySuggestionAdapter.test.ts` 覆盖非后端角色也能识别 `MQ`、`Redis`、`稳定性` 等通用技术主题并过滤已保存主题，且本地 fallback 候选会带 prompt version 和输入 hash；`CoachBoundarySuggestionFeedbackPage.test.tsx` 与 `boundarySuggestionFeedbackAdapter.test.ts` 覆盖候选采纳、拒绝、修订反馈、反馈原因和校准摘要，`MorePage.test.tsx` 与 `moreAdapter.test.ts` 覆盖 More 导出/导入保留边界候选反馈；`node tests/node_ai_tools_boundary_test.js`、`node tests/node_ai_routes_boundary_test.js`、`node tests/api_runtime_test.js` 覆盖 Node 工具、权限、HTTP runtime、schema、mock provider 成功、provider timeout fallback 和 `/api/coach/boundary-feedback` 权限/写入/读回/summary；`node tests/rust_ai_tools_boundary_test.js`、`node tests/rust_ai_routes_boundary_test.js`、`cargo test --manifest-path apps/rust-api/Cargo.toml boundary_suggestions_extract_common_topics_for_any_role --lib`、`cargo test --manifest-path apps/rust-api/Cargo.toml boundary_suggestions_use_provider_and_filter_existing_topics` 和 `cargo test --manifest-path apps/rust-api/Cargo.toml runtime_contract_matches_node_core_api` 覆盖 Rust 模块装配、路由边界、本地提取逻辑、mock provider 成功链路、`coach_boundary_feedback` 落库和读回。当前已覆盖候选提取、确认流、采纳后 provenance、React 本地候选反馈闭环、Node/Rust 服务端边界反馈账本和本地最小快速初始化；不等于远端真实 LLM 深度解析、批量导入知识边界、批量初始化审计或邀请制首登初始化。 |
| 产品迭代工作流 | `PASS` | `node tests/product_iteration_workflow_test.js` 覆盖 PRD 多版本缺失、根 `npm test` 不跑 React 测试、远端 coach evidence 缺角色追问库、fallback-only provider 只能 `PASS_WITH_LIMITS` 等退化会失败；`npm run validate:product-iteration -- --json` 当前返回 `PASS`，阻断问题和限制提示均为 0。该门禁已作为 `product_iteration_workflow` 接入 `npm run validate:delivery`，证明产品迭代流程文档和关键证据仍在；它不替代 Android HTTPS 或最终交付 readiness。 |
| 邀请账号生命周期 | `PASS_WITH_LIMITS` | `node tests/invitation_account_provisioning_test.js` 和 `cargo test --manifest-path apps/rust-api/Cargo.toml runtime_contract_matches_node_core_api` 已覆盖邀请记录批量导入、users file 开通/重置/禁用/恢复/删除、批量禁用/恢复/删除登录账号、邀请通知草稿、新用户登录、旧密码失效、禁用后拒绝登录、恢复后可登录、删除后拒绝登录、响应不含密码/hash、runtime 隔离，以及 `accountRole=owner` 被 Node/Rust 显式拒绝且不写入 users file；`node tests/auth_permissions_test.js` 和 Rust runtime contract 已覆盖邀请 `templateVersion`、批次状态更新和邀请记录删除；`apps/react-web/src/test/coachInvitationClient.test.ts` 覆盖批次报表 JSON 导出、邀请通知草稿、批量导入解析和批量账号动作请求；`docs/evidence/server-remote/coach-invitation-account.json` 已覆盖远端 users-file 生命周期 smoke、批量账号动作 smoke 和 `invitation_notifications_generated`；`docs/evidence/server-remote/login-switch.json` 已覆盖远端 UI owner 登录、退出、切换 smoke 用户登录、session/dataScope 读回和临时账号清理。该能力只适用于仓库外 `JOB_SPRINT_USERS_FILE`，`JOB_SPRINT_USERS_JSON` 模式会明确禁用页面开通；外部 SMTP/IM 自动发送和公开注册/组织租户未完成。 |
| AI provider 远端配置 | `PASS` | `npm run configure:remote-provider -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/provider-config.json` 已生成脱敏 `PASS` 报告，远端 systemd drop-in 已配置 DeepSeek/Anthropic-compatible provider，`job-sprint.service` 已 daemon-reload 并重启；报告只输出已配置 key 名称，不打印 token。随后 `npm run write:remote-coach-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/coach-artifacts.json` 已验证真实 provider 行为和 `llm_runs` 指标。 |
| public-safe 包 | `PASS_WITH_LIMITS` | 构建和扫描通过才可对外演示。 |
| server delivery package | `PASS` | 当前 `apps/rust-api/target/release/job-sprint-api` 为 Linux x86_64 ELF，SHA-256 为 `a785bef602c15959f77bdc08957e1de378d132d20ce55dc6a1d9ca5e21d0a89f`；`npm run build:server-delivery` 已生成匹配该 ELF 的同步包。 |
| server sync evidence | `PASS` | `npm run write:server-sync-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-sync/sync.json` 已通过，远端 manifest SHA-256 与本地一致：`e5a1d8195791d988b5e50a0c9badc310771d303dfe34a63167629f5919ae53f2`；`npm run restart:remote-service -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/service-restart.json` 已证明远端服务重启并读取同一 manifest，远端 DeepSeek coach artifact smoke 已通过。 |
| server remote evidence | `PASS` | `npm run write:remote-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/acceptance.json` 已证明远端页面、登录、session、health、`/api/progress remote save` 和 `remote readback` 均通过；`npm run write:remote-invitation-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/coach-invitations.json` 进一步证明 `/job-sprint/react/index.html`、React JS marker、`templateVersion=jd-focus-v1`、批量导入、批量状态更新和 `/api/coach/invitations` 新增/读回/删除通过；`npm run write:remote-invitation-account-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/coach-invitation-account.json --allow-create-account` 已证明远端 users-file 账号开通、smoke 用户登录、禁用/恢复/删除、批量账号动作、通知草稿生成和 runtime 隔离；`npm run write:remote-login-switch-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/login-switch.json --allow-create-account` 已证明远端 UI 登录、退出和换账号链路；这些 evidence 避免了旧进程和“按钮只在本地可用”的误判。 |
| final delivery runner | `PASS_WITH_LIMITS` | `npm run final:delivery -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/final-delivery/final-delivery.json --allow-create-account --allow-dirty` 已通过；release gate、server sync、server remote、Android remote、formal APK 和 post validation 均 PASS，限制为 dirty worktree 与 P8 理论性架构门禁。 |
| Android release-local APK | `PASS_WITH_LIMITS` | `app-release-signed-local.apk` 已通过 v2/v3 验签；这是本地临时签名，不等于生产签名或可无损覆盖已安装 debug 包。 |
| Android formal release gate | `PASS_WITH_LIMITS` | 最新 readiness 读取 `docs/evidence/android-release/formal-release.json` 并通过验签；APK 为 `apps/android/app/build/outputs/apk/release/app-release.apk`，APK SHA-256 为 `221c7483d288c5642304d51786fe6f8a664046c2079d3f8ad2df1dcc281d8343`。这证明正式 APK 候选可验签，不替代 Android 远端 HTTPS 真机 evidence。远端验收脚本和 readiness 现在会拒绝 flow/restart 快照或 auth session 状态落在 `file:///android_asset/...` 的报告，避免本地 fallback 冒充远端通过。 |
| 目标验收门禁 | `PASS_WITH_LIMITS` | HTTPS、服务器和 Android 生产链路目标已完成；P8 架构目标按门禁定义保留理论性限制。 |
| 最终交付 readiness | `PASS_WITH_LIMITS` | server delivery、server sync、server remote、Android remote、formal APK 和 final report 均通过；工作树未提交。 |
| 正式域名公网首访 | `PASS` | 私有交付 env 中的正式域名公网 TLS、证书 SAN、登录跳转和 HTTP 308 均通过。 |
| Codex AI 团队 | `PASS_WITH_LIMITS` | 作为全局对话层能力使用；不由 Job Sprint 项目脚本验收。 |

## P1 风险

| ID | 风险 | 影响 | 下一步 |
|---|---|---|---|
| R-001 | 正式域名证书续期或 SNI 回退 | 证书过期、DNS 漂移或 Nginx 配置回退会再次阻断 Web/Android | 续期后复测证书 SAN、HTTP 308、HTTPS 登录和 Android remote evidence。 |
| R-002 | Android remote evidence 随 APK 变化失效 | 旧报告不能证明新 APK 仍使用正式 HTTPS URL | 每次正式 APK 改动都复跑 Android 远端功能与杀进程读回，并绑定 APK hash。 |
| R-003 | public-safe 脱敏规则需持续维护 | 漏扫会暴露敏感部署线索 | 扩充云厂商、token、私钥、公司名扫描规则。 |
| R-004 | AI provider 运行保护仍不完整 | 已避免外部接口长期挂住请求，Rust/Node artifact 与 boundary suggestions 均有 fallback，boundary suggestions 已有 mock provider 成功和 timeout fallback；远端 DeepSeek provider 已配置并通过真实 smoke；仍缺真实 provider 错误分类、重试、熔断、成本监控和用户提示 | 基于当前 DeepSeek 证据继续补专项监控、错误分类和体验提示，并把真实 LLM 周复盘、机会状态和面试结果长期归因接入 `/api/coach/outcomes`。 |
| R-005 | 项目定位易被误读 | 新定位是泛 IT AI 求职教练，机会/JD 信号和规则版 JD 解析已影响建议，但当前仍不是公开 SaaS、企业 ATS、自动投递平台或高级 Java 后端主项目 | 对外统一使用 `docs/product/it-job-coach-v1/prd-recommended.md` 的定位：AI 原生求职教练叙事 + 邀请制收敛 MVP 工程边界。 |
| R-011 | 公网 HTTPS 依赖备案、DNS、证书和边界链路 | 任一环节回退都会同时影响 Web 与 Android | 当前已通过；把 2026-07-06 诊断报告保留为历史基线，证书续期或 DNS 变更后复跑正式域名与 Android remote evidence。 |
| R-012 | users file 账号开通配置边界被误用 | 如果把 `JOB_SPRINT_USERS_FILE` 放进仓库、复用内联 `JOB_SPRINT_USERS_JSON`，或在报告/API 中回显密码/hash，会把私人工具的邀请开通能力变成安全风险 | `JOB_SPRINT_USERS_FILE` 必须位于仓库外并尽量保持 `0600`；`JOB_SPRINT_USERS_JSON` 存在时页面开通必须禁用；API 响应、日志和 evidence 不得包含密码或 password hash；远端开通必须单独做脱敏 smoke。 |

## P2 风险

| ID | 风险 | 影响 | 下一步 |
|---|---|---|---|
| R-006 | Android remote 保存 Basic Auth 和 session 状态 | 远端模式仍需避免明文凭据、无边界 cookie 操作和过宽 JS bridge | 已补 host allowlist、跳转拦截、`AuthCredentialStore` 凭据集中边界、Android Keystore + AES/GCM 加密保存、旧明文键迁移清理、`AndroidBasicAuthController` Basic Auth 弹窗/认证 host 校验/已保存凭据尝试集中边界、`RemoteWebViewController` 远端/fallback 集中边界、`AndroidRemoteSettingsBridge` 远端 URL 设置边界、`AndroidAuthSettingsBridge` Basic Auth 状态/清除边界和 `AndroidSessionCookieBridge` session cookie 状态/清除/重新登录边界；cookie bridge 只对 `RemoteUrlPolicy` 判定可用的 HTTPS 远端 URL 生效，重新登录入口会跳转到同源 allowlist 下的 `/job-sprint/login.html`；下一步做完整远端 session/token 登录产品化。 |
| R-007 | 大文件维护成本高 | 改动容易回归 | 已从 Android Activity 抽出 `RemoteUrlPolicy`、`AuthCredentialStore`、`AndroidKeystoreStringCipher`、`RemoteWebViewController`、`AndroidRemoteSettingsBridge`、`AndroidAuthSettingsBridge`、`AndroidSessionCookieBridge`、`AndroidRecorderBridge`、`AndroidRecorderUploader`、`AndroidTranscribeEndpointResolver`、`AndroidSpeechServiceResolver`、`AndroidSpeechErrorPolicy`、`AndroidSpeechErrorCoordinator`、`AndroidSpeechSessionState`、`AndroidSpeechCallbackEmitter`、`AndroidSpeechStartCoordinator`、`AndroidSpeechRecognizerController`、`AndroidSpeechBridge`、`AndroidBasicAuthController`、`AndroidWebChromePermissionController`、`AndroidRemoteWebViewClient`、`AndroidActivityLifecycleController`、`AndroidWindowLayoutController`、`AndroidWebViewInitializer`、`AndroidAppStartupController`，从 Rust `lib.rs` 抽出 `app_bootstrap`、`runtime_store`、`runtime_records`、`runtime_routes`、`data_routes`、`application_routes`、`interview_mistake_routes`、`ai_routes`、`static_routes`、`static_files`、`http_responses`、`login_rate`、`session_token`、`auth_config`、`auth_bearer`、`auth_tokens`、`auth_users`、`auth_values`、`auth_hash`、`auth_permissions`、`auth_state`、`auth_http`、`auth_routes`、`ai_tools`、`ai_transcribe`，从 Node 兼容服务抽出 `auth.js`、`auth_routes.js`、`runtime_store.js`、`runtime_routes.js`、`static_files.js`、`ai_routes.js`、`ai_tools.js` 和 `http_utils.js`；新增 `npm run validate:architecture-quality` 自动校验拆分模块、关键入口行数预算和 legacy JS/fallback hash 一致性，已接入 `npm test`；Node 兼容入口已接近薄路由层，后续转向远端 session/token 产品化和剩余 JS bridge 最小化，冻结 legacy JS。 |
| R-008 | 更多页导入/恢复只覆盖 React 主状态 | 学习重点和面试薄弱题仍由独立 localStorage key 管理，不属于 `jobSprint.react.v1` 导入范围 | 若要做完整设备迁移，再设计 `jobSprint.backup.v1` 聚合备份格式。 |
| R-009 | Android 正式签名材料需要长期保管 | release-local APK 不能冒充正式包；更换签名会导致已安装正式包无法无损升级；仓库内 keystore 会被构建和 readiness 拒绝 | 当前仓库外 keystore 位于 `~/.job-sprint/android-release/job-sprint-release.p12`，私有 env 位于 `~/.job-sprint/job-sprint-delivery.env`，已产出 `FORMAL_SIGNED` 报告；后续必须保管该 keystore 和密码，不要提交到仓库。 |
| R-010 | 最终交付输入或证据缺失导致误报 | 当前证据已齐备，但后续源码、Rust ELF、React assets、APK 或证书变化都会使旧报告失效 | 每次收口先跑 `npm run validate:delivery` 与最终 runner；必须同时绑定 server manifest、APK hash、authenticated Android remote evidence 和 `FORMAL_SIGNED` 报告。 |

## 删除旧文档后的验收口径

- 当前事实以 `docs/core/` 为准。
- 旧文档路径不得作为 PASS 证据。
- 若命令、测试或数据仍引用旧文档，必须改到 `docs/core/` 或保留必要运行文档。
- 删除截图或报告前，必须保留最新验收摘要、关键命令和剩余风险。

## 2026-07-10 产品级 UI 基线

- 已验证范围：全局产品壳、Today 新用户/任务态、React→Android 同源 assets、Android 状态栏与 WebView 顶部 inset。
- Web evidence：28 个测试文件、95 条测试通过，类型检查与生产构建通过；1440px/390px 新用户与任务态已截图复核。
- Android evidence：正式签名 release v2/v3 通过并覆盖安装；本地 WebView 全功能流、杀进程读回、384 CSS px 无横向溢出、44px 触控门和键盘态通过。
- P0 追加：准备页已拆为 4 阶段，Android `384 × 792` 下从 11171px 降为 2680px；阶段焦点、3 次物理上滑到底和 IME 隐藏底栏通过。
- 边界：P5 已补远端部署、全项目 UI 回归和正式签名 APK；仍需用真实冷启动指标决定 bundle 拆分，并在提交后重跑同一门禁。

## 2026-07-20 GitFlow 自动收口基线

- 普通日更完成定义已从“提交或 Draft PR”升级为“合入 `develop`、删除短分支、工作树干净”；开放 PR、Draft 或冲突存在时禁止再创建新需求分支。
- 正式主分支为 `main`。定期发布只允许 `release/* -> main`，触发条件为距上次 release 满 7 天、累计 3 项需求或用户明确要求；禁止直接创建 `develop -> main` PR。
- `.github/gitflow-automation-contract.json` 是积压阻断、目标分支、合并方式、发布阈值与回同步目标的机器事实源；`.github/workflows/gitflow-policy.yml` 的 `validate` 除 GitFlow 策略测试外，还执行产品迭代合同测试/校验与敏感扫描器测试/扫描。
- 该基线约束 Git 仓库合并与发布，不代表服务器、Android APK 或生产数据已自动部署；这些仍按最终交付门禁单独验收。
