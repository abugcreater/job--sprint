# 目标验收审计

日期：2026-07-07

## 2026-07-11 最终交付更新

本轮 HTTPS 生产目标已完成：仓库外私有 env 注入的正式域名公网 TLS 与 HTTP 308 通过，最新 React/Rust 已部署并重启服务，Web 登录/session/写入读回通过；正式 APK 已关闭 cleartext、注入同一 HTTPS React URL并通过 v2/v3 验签；OnePlus 8 Pro 已完成远端 HTTPS 登录、业务写入、AI 草稿处理和杀进程重启读回。最终 runner 为 `PASS_WITH_LIMITS`，限制仅来自未提交工作树与 P8 理论性架构门禁。下文 2026-07-07 的 HTTP、443 reset、Android evidence 缺失等结论是历史审计快照，不得作为当前状态。

## 审计结论

本轮目标按最新证据收口为 `PASS_WITH_LIMITS`：Web、Android、Rust/SQLite、Linux x86_64 server-delivery、服务器同步与重启、HTTPS 远端写入读回、远端 UI 登录、DeepSeek coach evidence、正式 APK 和 Android HTTPS 真机 evidence 均已通过。限制来自未提交工作树和 P8 理论性架构门禁。

2026-07-07 追加补证：邀请账号管理已从单条台账继续补到小范围试用后台的最小动作集。React 已支持批次筛选、批量导入、批量状态更新、邀请报表 JSON 导出、首登模板版本、邀请记录删除、可复制邀请通知草稿、users-file 账号禁用/恢复/删除和批量账号禁用/恢复/删除；Node/Rust 均支持 `templateVersion` 持久化、`bulk-import`、批次状态更新、删除邀请记录、单账号状态动作、`account-batch-status` 批量登录账号动作和 `notification-draft` 通知草稿；已用 React 单测、Node 权限合同、Node/Rust 架构边界测试、Rust runtime contract、远端账号生命周期 smoke 和远端 UI 登录切换 smoke 覆盖。DeepSeek/Anthropic-compatible provider 已通过 `provider-config.json` 和 `coach-artifacts.json` 远端证据，证明真实模型调用、schema、token/延迟/成本字段、feedback summary 和 outcomes GET/POST 可用。该补证不等于外部 SMTP/IM 自动发送、公开注册、组织租户、真实 LLM 深度 JD 解析质量闭环、真实 LLM 周复盘、机会/面试结果长期归因或 Android HTTPS 交付完成。

## Team Room 执行状态

| 项目 | 结论 |
|---|---|
| 入口 | `manager-dispatch` |
| 团队状态 | `TEAM_ROOM_PARTIAL`，不是完整 Team Room |
| 本轮真实派发 agent | 无 |
| 未派发原因 | 当前运行时已有历史 agent 卡在关闭流程，且补派专家可能触发 `agent thread limit reached`；继续 close/wait 会扩大卡顿。本轮采用主线程熔断收口和可验证命令，避免把 agent 生命周期当成交付前置条件。 |
| 不能宣称的内容 | 不能宣称完整多 agent 团队通过，不能把主线程分角色总结冒充真实 agent 协作。 |
| 复盘继承 | 当前线程进入 `current_thread_quarantine=true`，`max_agents=0`；遇到 `agent thread limit reached`、`closing agents` 或“正在关闭智能体”必须立即熔断：不补派、不关闭、不等待，记录为 `spawn_failed` 或 `stale_or_missing` 后切回主线程 Manager Dispatch；简单缺口不全员参与；缺少原始 agent 证据时只标 `TEAM_ROOM_PARTIAL`；备案完成前，远端验收、Android 远端 evidence 和最终报告保留 `USER_ACTION_REQUIRED`，但不作为代码完成阻塞项。 |

## 用户目标验收矩阵

| 目标 | 结论 | 证据与边界 |
|---|---|---|
| Web 与 Android 功能对齐 | `PASS` | React Web、Android 本地 WebView 与正式 HTTPS 远端 WebView 均覆盖今日、证据、延期、学习、面试、投递、复盘、导入/导出和重启读回；远端报告包含 authenticated session 且所有快照保持正式 HTTPS URL。 |
| 用户、进度、复盘等 DB 存储 | `PASS` | Rust/SQLite 已用 `users` 与 `runtime_items` 持久化用户态、`progress`、`reviews`、`applications`、`interview_mistakes`，延期记录存放在 `progress.delayRecords` JSON 内；本轮 DB 存储目标已由 Rust/SQLite 合同和 UI 入库测试证明。 |
| DB 化后的全功能测试 | `PASS` | `npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional` 均已覆盖延期记录、更多页导出/导入恢复和持久化读回；`npm run validate:functional-coverage` 已把 Web、Android WebView、Rust/SQLite UI 三条测试链的今日、延期、学习、面试、投递、复盘、更多页、重启读回和 SQLite 入库覆盖做成防退化门禁；Rust/SQLite 测试已证明导入恢复记录进入 `runtime_items`，Rust 后端合同测试已直接查询 SQLite 确认 Bearer token 写入的 `progress.bearer-block` 落到 `runtime_items(scope='kai', item_key='progress')`。 |
| 前端/后端概念 | `PASS` | 前端为 React 19 + TypeScript + Vite + Zustand，Android 通过 WebView 复用 React assets；后端目标入口为 Rust + Axum + SQLx + SQLite，Node 保留兼容和回滚。 |
| 架构与技术栈市场认可度 | `PASS_WITH_LIMITS` | React、TypeScript、Rust、SQLite、Android WebView 可作为个人效率工具与 AI 工程化辅助项目展示；高级 Java 后端求职主线仍应依赖 Spring Boot、Spring Cloud、MyBatis、MySQL、Redis、MQ、JVM 和稳定性治理项目。 |
| P8 级代码质量与渐进重构 | `PASS_WITH_LIMITS` | 当前未发现阻断级缺陷。本轮新增 `npm run validate:architecture-quality`，自动检查 Rust/Android/Node 拆分模块仍存在、关键入口保持薄层、legacy `assets/schedule.js` 与 Android fallback 拷贝 hash 一致且行数不继续膨胀，并已接入 `npm test`。Android/Rust/Node 已完成多轮语义拆分并有边界测试；Android 远端 session/token 产品化仍需继续收敛。后续应继续按 AST/语义渐进推进远端 session/token 和 JS bridge 最小化，冻结 legacy Node/JS，不做推倒重写。 |
| 重构后的全功能测试 | `PASS_WITH_LIMITS` | Rust/Node/Android 边界测试、`npm test`、Web/Rust/Android 本地功能链路、HTTPS 远端 Android 流与交付 readiness 已通过；限制为 P8 理论性架构门禁。 |
| 同步服务器与正式 APK | `PASS` | Linux x86_64 ELF、server delivery、服务器同步与重启、HTTPS 远端写入读回、正式 APK 和 Android remote evidence 均通过；manifest 与 APK hash 已绑定最新产物。 |

## 本轮新增功能证据

| 能力 | 验证命令 | 结果 |
|---|---|---|
| React 延期记录 | `npm run test:functional` | 通过；浏览器重启后仍读到 `功能测试延期原因`。 |
| Rust/SQLite 延期入库 | `npm run test:rust:functional` | 通过；SQLite `runtime_items.progress.delayRecords` 读到 `Rust SQLite 延期原因`。 |
| Android 延期记录 | `npm run test:android:functional` | 通过；真机 WebView 杀进程重启后仍读到 `Android 延期原因`，localStorage hash 稳定。 |
| 更多页导出/导入恢复 | `npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional` | Web 导入后 `/api/runtime` 读到 `导入恢复证据` 和 `导入恢复延期原因`；Rust/SQLite 入库读到 `Rust SQLite 导入恢复证据`；Android WebView 已确认导入控件存在。 |
| Android URL 策略拆分 | `npm test`、`gradle -p apps/android :app:assembleDebug`、`npm run test:android:functional` | 通过；`RemoteUrlPolicy` 承接远端 URL 策略，`MainActivity.java` 从 1311 行降到 1271 行，真机 WebView 本地入口和重启读回仍通过。 |
| Android 凭据存储边界拆分 | `npm test`、`npm test --prefix apps/react-web`、`npm run test:functional`、`npm run test:rust:functional`、`gradle -p apps/android :app:assembleDebug`、`adb install -r apps/android/app/build/outputs/apk/debug/app-debug.apk`、`npm run test:android:functional` | 通过；`AuthCredentialStore` 承接远端 URL 保存、Basic Auth 保存/清除和认证重试去重，`MainActivity.java` 从 1271 行降到 1239 行，Android 本地 WebView 全流程和杀进程重启读回仍通过。 |
| Android Basic Auth 加密存储 | `node tests/android_auth_credential_store_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac`、`npm test`、`npm run test:android:functional` | 通过；Basic Auth 用户名/密码改为 Android Keystore + AES/GCM 加密保存，保留旧明文键迁移后清理；敏感扫描未发现高风险命中，Android 本地 WebView 杀进程重启读回仍通过。 |
| Android KeystoreStringCipher 拆分 | `node --check tests/android_auth_credential_store_test.js`、`node tests/android_auth_credential_store_test.js`、`node tests/android_basic_auth_controller_test.js`、`node tests/android_settings_bridge_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional`、`git diff --check`、`npm run validate:delivery -- --allow-dirty` | 通过；`AndroidKeystoreStringCipher` 承接 Android Keystore、AES/GCM、IV 和 Base64 编解码细节，`AuthCredentialStore` 只保留 preference key、远端 URL、Basic Auth 保存/清除、旧明文键迁移和认证重试去重；`AuthCredentialStore.java` 从 202 行降到 132 行。 |
| Android BasicAuthController 拆分 | `node tests/android_basic_auth_controller_test.js`、`node tests/android_auth_credential_store_test.js`、`node tests/android_remote_url_policy_test.js`、`node tests/android_remote_webview_controller_test.js`、`node tests/android_settings_bridge_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidBasicAuthController` 承接 Basic Auth host allowlist、已保存凭据尝试、登录弹窗、保存/清除凭据和取消 fallback，`MainActivity.java` 从 321 行降到 242 行。 |
| Android WebChrome 权限控制器拆分 | `node tests/android_web_chrome_permission_controller_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/android_recorder_bridge_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidWebChromePermissionController` 承接 WebView 音频采集权限请求、非音频资源拒绝和系统录音权限申请，`MainActivity.java` 从 242 行降到 213 行。 |
| Android RemoteWebViewClient 拆分 | `node tests/android_remote_webview_client_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/android_remote_url_policy_test.js`、`node tests/android_basic_auth_controller_test.js`、`node tests/android_remote_webview_controller_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidRemoteWebViewClient` 承接 Basic Auth 委托、URL 拦截、页面 URL 跟踪、主帧错误 fallback 和 SSL 拒绝，`MainActivity.java` 从 213 行降到 167 行。 |
| Android ActivityLifecycleController 拆分 | `node tests/android_activity_lifecycle_controller_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/android_recorder_bridge_test.js`、`node tests/android_remote_webview_client_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidActivityLifecycleController` 承接音频权限回调、暂停取消、销毁释放和返回键处理，`MainActivity.java` 从 167 行降到 158 行。 |
| Android WindowLayoutController 拆分 | `node tests/android_window_layout_controller_test.js`、`node tests/android_activity_lifecycle_controller_test.js`、`node tests/android_remote_webview_client_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidWindowLayoutController` 承接窗口颜色、系统栏 flags、根 WebView 布局和 system bar insets，`MainActivity.java` 从 158 行降到 106 行。 |
| Android WebViewInitializer 拆分 | `node tests/android_webview_initializer_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/android_recorder_bridge_test.js`、`node tests/android_settings_bridge_test.js`、`node tests/android_web_chrome_permission_controller_test.js`、`node tests/android_remote_webview_client_test.js`、`node tests/android_activity_lifecycle_controller_test.js`、`node tests/speech_provider_test.js`、`node tests/android_window_layout_controller_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidWebViewInitializer` 承接 WebSettings、JS bridge 绑定、WebChromeClient/WebViewClient 装配和 lifecycle controller 创建，`MainActivity.java` 从 106 行降到 84 行。 |
| Android AppStartupController 拆分 | `node tests/android_app_startup_controller_test.js`、`node tests/android_window_layout_controller_test.js`、`node tests/android_webview_initializer_test.js`、`node tests/android_remote_webview_controller_test.js`、`node tests/android_auth_credential_store_test.js`、`node tests/android_basic_auth_controller_test.js`、`node tests/android_activity_lifecycle_controller_test.js`、`node tests/speech_provider_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidAppStartupController` 承接启动编排、控制器组装和默认远端/本地加载决策，`MainActivity.java` 从 84 行降到 53 行。 |
| Android RemoteWebViewController 拆分 | `node --check tests/android_remote_webview_controller_test.js && node tests/android_remote_webview_controller_test.js`、`npm test`、`gradle -p apps/android :app:assembleDebug`、`npm run test:functional`、`npm run test:rust:functional` | 通过；`RemoteWebViewController` 承接远端 URL 选择、本地 React asset fallback、离线 fallback、WebSettings 安全切换和页面 URL 跟踪，`MainActivity.java` 从 1239 行降到 1166 行；本轮未重跑 Android 真机，因此不扩大为远端真机首访通过。 |
| Android Remote/Auth SettingsBridge 拆分 | `node --check assets/schedule.js`、`node --check apps/android/app/src/main/assets/web/assets/schedule.js`、`node --check tests/android_settings_bridge_test.js`、`node --check tests/android_webview_initializer_test.js`、`node --check tests/android_speech_bridge_test.js`、`node tests/android_settings_bridge_test.js`、`node tests/android_webview_initializer_test.js`、`node tests/android_speech_bridge_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional`、`git diff --check`、`npm run validate:delivery -- --allow-dirty` | 通过；`AndroidRemoteSettingsBridge` 承接远端 URL 设置、reload remote 和 fallback 切换，`AndroidAuthSettingsBridge` 承接 Basic Auth 状态读取和清除；Android 内置 Web asset 已同步使用 `window.AndroidRemoteSettings`、`window.AndroidAuthSettings` 与 `window.AndroidSessionCookies`，且与根目录 `assets/schedule.js` 字节级一致；本轮未改语音/录音本体，Android 本地 WebView 杀进程重启读回仍通过；最终交付门禁仍因远端 URL/认证、Android 远端 evidence 和正式签名输入缺失返回 `USER_ACTION_REQUIRED`。 |
| Android session 重新登录入口 | `node --check assets/schedule.js`、`node --check apps/android/app/src/main/assets/web/assets/schedule.js`、`node tests/android_settings_bridge_test.js`、`node tests/android_remote_url_policy_test.js`、`node tests/android_remote_webview_controller_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`RemoteUrlPolicy.loginUrlFor` 生成 allowlist 远端 `/job-sprint/login.html?next=...`，`RemoteWebViewController.loadLoginOrFallback` 负责加载登录页，`AndroidSessionCookieBridge.clearSessionAndOpenLogin` 清除 `job_sprint_session` 后打开登录页；本轮未获得真实远端 URL/账号，因此不扩大为 Android 远端登录验收通过。 |
| Web/Android 功能对齐门禁 | `node --check tools/validate_feature_parity.js`、`node --check tools/delivery_readiness_feature_parity.js`、`node tests/feature_parity_test.js`、`npm run validate:feature-parity` | 通过；功能对齐门禁按 9 项矩阵检查登录/session 能力入口、今日证据、延期记录、学习、面试、投递、复盘、更多页导入和重启读回，并已作为 `feature_parity` 直接接入 `npm run validate:delivery`；本地 Android evidence 现在必须包含 `authEvidence.mode=local` 和至少一条 `sessionStates` 采样，避免旧结构报告只靠源码 token 证明登录/session 链路；Android 远端 HTTPS 真机登录和保存 evidence 仍由 `npm run test:android:remote:functional` 单独证明，缺失时目标验收仍保持 `USER_ACTION_REQUIRED`。 |
| Android 远端认证 evidence 门禁 | `node --check tests/android_webview_functional_persistence_test.js`、`node --check tools/validate_final_delivery_readiness.js`、`node tests/android_remote_functional_entrypoint_test.js`、`node tests/final_delivery_readiness_android_remote_test.js` | 通过；`npm run test:android:remote:functional` 生成的报告新增 `authEvidence`，记录账号密码是否配置、是否看到登录页、是否尝试登录、每次页面切换的 `/api/auth/session` authenticated 状态和 Android session cookie 观测；`npm run validate:delivery` 现在会拒绝缺少 `authEvidence`、未配置账号密码或没有 authenticated session 状态的 Android 远端报告，避免把仅有 HTTPS/storage hash 的报告误标为远端登录通过。 |
| Android RecorderBridge 初始拆分 | `node --check tests/android_recorder_bridge_test.js && node tests/android_recorder_bridge_test.js`、`node tests/speech_provider_test.js`、`node tests/android_speech_bridge_test.js`、`gradle -p apps/android :app:assembleDebug` | 通过；当轮把录音桥接从 `MainActivity` 中拆出，`MainActivity.java` 从 1129 行降到 832 行；当前上传线程和 multipart 细节已继续下沉到 `AndroidRecorderUploader`。 |
| Android RecorderUploader 拆分 | `node tests/android_recorder_uploader_test.js`、`node tests/android_recorder_bridge_test.js`、`node tests/speech_provider_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidRecorderUploader` 承接录音 multipart 上传、cookie 转发、响应解析和后台上传线程，`AndroidRecorderBridge.java` 从 294 行降到 225 行。 |
| Android TranscribeEndpointResolver 拆分 | `node tests/android_transcribe_endpoint_resolver_test.js`、`node tests/android_recorder_bridge_test.js`、`node tests/speech_provider_test.js`、`node tests/android_speech_bridge_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidTranscribeEndpointResolver` 承接当前页面优先、配置 URL fallback 和 `/api/transcribe` endpoint 推导，`AndroidRecorderBridge` 保留录音、上传和回调职责。 |
| Android SpeechServiceResolver 拆分 | `node tests/android_speech_service_resolver_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/speech_provider_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidSpeechServiceResolver` 承接系统语音服务可用性、默认服务选择和 queryable `RecognitionService` fallback，`AndroidSpeechBridge` 保留识别状态机、权限续跑和 JS 回调职责。 |
| Android SpeechErrorPolicy 拆分 | `node tests/android_speech_error_policy_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/speech_provider_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidSpeechErrorPolicy` 承接语音错误码、错误文案、冷却时长和可重试判断，`AndroidSpeechBridge` 当轮保留识别状态机和 JS 回调职责。 |
| Android SpeechErrorCoordinator 拆分 | `node tests/android_speech_error_coordinator_test.js`、`node tests/android_speech_error_policy_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/speech_provider_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional`、`git diff --check`、`npm run validate:delivery -- --allow-dirty` | 通过；`AndroidSpeechErrorCoordinator` 承接语音启动失败映射、错误策略消费、冷却启动和连续失败文案，`AndroidSpeechBridge` 不再直接持有 `AndroidSpeechErrorPolicy` 或 `ActivityNotFoundException`；Web 持久化、Rust SQLite 入库和 Android WebView 杀进程重启读回仍通过，交付门禁仍因远端 URL/认证、Android 远端 evidence 和正式签名输入缺失返回 `USER_ACTION_REQUIRED`。 |
| Android SpeechSessionState 拆分 | `node tests/android_speech_session_state_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/speech_provider_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidSpeechSessionState` 承接语音 phase、listening、ready、cooldown、debounce 和连续失败计数，`AndroidSpeechBridge.java` 从 382 行降到 359 行。 |
| Android SpeechCallbackEmitter 拆分 | `node tests/android_speech_callback_emitter_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/speech_provider_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac` | 通过；`AndroidSpeechCallbackEmitter` 承接语音状态、partial、final、error 的 JS 回调、payload 拼装和 WebView evaluate，`AndroidSpeechBridge.java` 从 359 行降到 317 行。 |
| Android SpeechStartCoordinator 拆分 | `node tests/android_speech_start_coordinator_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/android_speech_session_state_test.js`、`node tests/speech_provider_test.js`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional`、`git diff --check`、`npm run validate:delivery -- --allow-dirty` | 通过；`AndroidSpeechStartCoordinator` 承接语音开始前的冷却、debounce、服务可用性、麦克风权限请求和授权后续跑，`AndroidSpeechBridge` 不再直接依赖 `Manifest.permission.RECORD_AUDIO`、`PackageManager.PERMISSION_GRANTED` 或 `requestPermissions`；Web 持久化、Rust SQLite 入库和 Android WebView 杀进程重启读回仍通过，交付门禁仍因远端 URL/认证、Android 远端 evidence 和正式签名输入缺失返回 `USER_ACTION_REQUIRED`。 |
| Android SpeechRecognizerController 拆分 | `node tests/android_speech_recognizer_controller_test.js`、`node tests/android_speech_bridge_test.js`、`node tests/android_speech_service_resolver_test.js`、`node tests/speech_provider_test.js`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional`、`gradle -p apps/android :app:compileDebugJavaWithJavac :app:compileReleaseJavaWithJavac`、`npm run validate:delivery -- --allow-dirty` | 通过；`AndroidSpeechRecognizerController` 承接 `SpeechRecognizer` 创建、`RecognizerIntent` 构造、recognizer listener 和结果提取，`AndroidSpeechBridge.java` 从 317 行降到 255 行；Web 持久化、Rust SQLite 入库和 Android WebView 杀进程重启读回仍通过，交付门禁仍因远端验收和正式签名输入缺失返回 `USER_ACTION_REQUIRED`。 |
| Android SpeechBridge 拆分 | `node tests/android_speech_bridge_test.js`、`node tests/speech_provider_test.js`、`gradle -p apps/android :app:assembleDebug` | 通过；`AndroidSpeechBridge` 承接原生语音识别状态机、权限续跑和 JS 回调，`MainActivity.java` 从 832 行降到 321 行；本轮未重跑 Android 真机语音。 |
| Rust app bootstrap 拆分 | `node --check tests/rust_app_bootstrap_boundary_test.js && node tests/rust_app_bootstrap_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional` | 通过；`app_bootstrap.rs` 承接 Axum 启动、SQLite connect/init、用户同步、legacy JSON 迁移、body limit 和监听入口，`apps/rust-api/src/lib.rs` 从 253 行降到 92 行。 |
| Rust runtime store 拆分 | `cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:rust:functional`、`node tests/rust_runtime_store_boundary_test.js` | 通过；`runtime_store.rs` 承接 SQLite runtime item 读写、legacy JSON 迁移和 runtime payload 规范化，`apps/rust-api/src/lib.rs` 从 2203 行降到 2024 行，Rust runtime contract 和 SQLite UI 入库保持通过。 |
| Rust static routes 拆分 | `node --check tests/rust_static_routes_boundary_test.js && node tests/rust_static_routes_boundary_test.js`、`node tests/rust_static_files_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional` | 通过；`static_routes.rs` 承接静态 fallback HTTP 分支、未登录跳转、public-safe 授权选择和文件读取响应，`static_files.rs` 继续保持纯路径/content-type/no-store 判定，`apps/rust-api/src/lib.rs` 从 310 行降到 253 行。 |
| Rust static files 拆分 | `node --check tests/rust_static_files_boundary_test.js && node tests/rust_static_files_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional` | 通过；`static_files.rs` 承接静态路径、public-safe fallback、content-type 和 no-store 判定，并通过边界测试确认该模块不依赖 `AuthState` 或 `auth_has_permission`。 |
| Rust HTTP responses 拆分 | `node --check tests/rust_http_responses_boundary_test.js && node tests/rust_http_responses_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:rust:functional` | 通过；`http_responses.rs` 承接统一响应、安全 header 和动态 header 写入，边界测试确认相关工具不再回流到 `lib.rs`。 |
| Rust login rate 拆分 | `node --check tests/rust_login_rate_boundary_test.js && node tests/rust_login_rate_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional` | 通过；`login_rate.rs` 承接登录失败限流存储、key 归一化、窗口和计数逻辑，并复用 `auth_hash::sha256_hex` 生成用户名 hash 片段，Rust 单测覆盖 forwarded IP 与用户名归一化、失败记录和清理，边界测试确认该模块不依赖 `AppState` 且不再直接依赖 `sha2` 或定义本地 `sha256_hex`。 |
| Rust session token 拆分 | `node --check tests/rust_session_token_boundary_test.js && node tests/rust_session_token_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional` | 通过；`session_token.rs` 承接 session 签名、cookie 生成/读取、Secure 判断和 payload 验签，Rust 单测覆盖签名 roundtrip、错误 secret、cookie encode/decode 和 `x-forwarded-proto`，边界测试确认 session token 工具不再回流到 `lib.rs`。 |
| Rust auth users/values 拆分 | `node --check tests/rust_auth_config_boundary_test.js && node tests/rust_auth_config_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test` | 通过；`auth_users.rs` 承接 `UserConfig`、禁用认证用户、legacy env 用户、用户 JSON 解析、用户索引和 `dataScope` 选择，`auth_values.rs` 承接认证配置 JSON 字段读取，`auth_config.rs` 通过 `pub(crate) use crate::auth_users::UserConfig` 保持调用面兼容并从 373 行降到 200 行。 |
| Rust auth tokens 拆分 | `node --check tests/rust_auth_config_boundary_test.js && node tests/rust_auth_config_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test` | 通过；`auth_tokens.rs` 承接 `BearerToken`、bearer token env/file 读取、JSON 解析、token hash 规范化和 known user 过滤，`auth_config.rs` 通过 `pub(crate) use crate::auth_tokens::BearerToken` 保持调用面兼容并从 197 行降到 118 行。 |
| Rust auth bearer 拆分 | `node --check tests/rust_auth_state_boundary_test.js && node tests/rust_auth_state_boundary_test.js`、`node --check tests/rust_auth_config_boundary_test.js && node tests/rust_auth_config_boundary_test.js`、`node --check tests/rust_auth_routes_boundary_test.js && node tests/rust_auth_routes_boundary_test.js`、`node --check tests/rust_session_token_boundary_test.js && node tests/rust_session_token_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test` | 通过；`auth_bearer.rs` 承接 Authorization Bearer 解析、空 token/坏 token 拒绝、token hash constant-time 对比、`expiresAt` 过期判断和 bearer user 映射，`auth_state.rs` 只保留 session 校验与认证状态编排，`auth_hash.rs` 承接 `constant_time_eq`，边界测试确认 Authorization header、`sha256_hex`、`DateTime` 和 bearer token 细节不再回流到 `auth_state.rs`，后端合同测试直接查询 SQLite 证明 bearer 写入的 `progress.bearer-block` 已落到 `runtime_items`，`auth_state.rs` 从 218 行降到 169 行。 |
| Rust auth hash 拆分 | `node --check tests/rust_auth_config_boundary_test.js && node tests/rust_auth_config_boundary_test.js`、`node --check tests/rust_login_rate_boundary_test.js && node tests/rust_login_rate_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test` | 通过；`auth_hash.rs` 承接 SHA-256、64 位 hex digest 校验和 constant-time 字符串比较，`auth_config.rs` 与 `login_rate.rs` 不再直接依赖 `sha2`，边界测试确认 `auth_routes.rs`、`auth_bearer.rs` 与 `login_rate.rs` 从 `auth_hash` 获取 hash/比较工具；`auth_config.rs` 从 382 行降到 373 行，Rust 主入口保持薄路由层。 |
| Rust auth permissions 拆分 | `node --check tests/rust_auth_state_boundary_test.js && node tests/rust_auth_state_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test` | 通过；`auth_permissions.rs` 承接角色权限归一化、通配权限和只读判断，Rust 单测覆盖 viewer 权限合并、去重、extra permission 和 read-only 判定，边界测试确认 `auth_state.rs` 不再持有 `HashSet`、`normalize_permissions` 或 `role_permissions`，`auth_state.rs` 从 349 行降到 289 行。 |
| Rust auth state 拆分 | `node --check tests/rust_auth_state_boundary_test.js && node tests/rust_auth_state_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional` | 通过；`auth_state.rs` 承接运行时 session 校验、认证状态编排和 `dataScope`，Bearer token 运行时校验已下沉到 `auth_bearer.rs`，角色权限和只读判定已下沉到 `auth_permissions.rs`，`apps/rust-api/src/lib.rs` 保持薄路由层。 |
| Rust auth HTTP 响应拆分 | `node --check tests/rust_auth_state_boundary_test.js && node tests/rust_auth_state_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test` | 通过；`auth_http.rs` 承接认证错误响应、runtime 响应外壳、read-only 标记输出和静态入口未登录跳转，`auth_state.rs` 通过 re-export 保持外部调用面兼容并继续聚焦 session 认证状态、`dataScope` 和权限上下文；边界测试确认 redirect、`request_base`、`Map::new`、`readOnly` 响应组装不再回流到 `auth_state.rs`，`auth_state.rs` 从 289 行降到 218 行。 |
| Rust auth routes 拆分 | `node --check tests/rust_auth_routes_boundary_test.js && node tests/rust_auth_routes_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional` | 通过；`auth_routes.rs` 承接 `/api/auth/session`、`/api/auth/login`、`/api/auth/logout` HTTP handler，`apps/rust-api/src/lib.rs` 从 921 行降到 795 行。 |
| Rust runtime routes 拆分 | `node --check tests/rust_runtime_routes_boundary_test.js && node tests/rust_runtime_routes_boundary_test.js`、`node tests/rust_runtime_store_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional` | 通过；`runtime_routes.rs` 承接 `/api/runtime`、`/api/progress`、`/api/reviews` HTTP handler，继续复用 `runtime_store.rs` 的 SQLite runtime item helper，`apps/rust-api/src/lib.rs` 从 795 行降到 695 行。 |
| Rust runtime records 拆分 | `node --check tests/rust_data_routes_boundary_test.js && node tests/rust_data_routes_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test` | 通过；`runtime_records.rs` 承接 runtime 记录 `id` 与 `createdAt` 规范化、非对象 payload 兜底和记录 ID 生成，Rust 单测覆盖新增缺省字段和保留既有字段；边界测试确认 `data_routes.rs` 不再持有 `Uuid`、`Utc`、`normalize_record`、`object_value` 或 `make_id` 实现，`data_routes.rs` 从 342 行降到 307 行。 |
| Rust data resource routes 拆分 | `node --check tests/rust_data_routes_boundary_test.js && node tests/rust_data_routes_boundary_test.js`、`node tests/rust_runtime_store_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional` | 通过；`application_routes.rs` 承接 `/api/applications` CRUD、`interview_mistake_routes.rs` 承接 `/api/interview-mistakes` CRUD，`data_routes.rs` 只保留 re-export facade，路由表调用面保持 `data_routes::...` 兼容；边界测试确认认证、读写、记录规范化和 SQLite runtime item 写入逻辑不再回流到 `data_routes.rs`，`data_routes.rs` 从 307 行降到 6 行。 |
| Rust AI routes 拆分 | `node --check tests/rust_ai_routes_boundary_test.js && node tests/rust_ai_routes_boundary_test.js`、`node tests/rust_ai_tools_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional` | 通过；`ai_routes.rs` 承接 `/api/score-answer`、`/api/generate-kb`、`/api/transcribe` HTTP handler，`ai_tools.rs` 保持 AI 评分/知识库纯 payload/fallback 判定，`ai_transcribe.rs` 保持 ASR 上传校验和 multipart 音频解析，`apps/rust-api/src/lib.rs` 从 374 行降到 310 行。 |
| Rust AI tools 拆分 | `node --check tests/rust_ai_tools_boundary_test.js && node tests/rust_ai_tools_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test`、`npm run test:functional`、`npm run test:rust:functional` | 通过；`ai_tools.rs` 承接 AI 评分 fallback 和知识库生成 fallback，Rust 单测覆盖评分入参、provider fallback warning 和知识库生成 provider fallback warning，边界测试确认该模块不依赖鉴权、SQLite、AppState 或 ASR 上传解析。 |
| Rust AI transcribe 拆分 | `node --check tests/rust_ai_tools_boundary_test.js && node tests/rust_ai_tools_boundary_test.js`、`node --check tests/rust_ai_routes_boundary_test.js && node tests/rust_ai_routes_boundary_test.js`、`cargo fmt --manifest-path apps/rust-api/Cargo.toml -- --check`、`cargo clippy --manifest-path apps/rust-api/Cargo.toml --all-targets -- -D warnings`、`cargo test --manifest-path apps/rust-api/Cargo.toml`、`cargo build --release --manifest-path apps/rust-api/Cargo.toml`、`npm test` | 通过；`ai_transcribe.rs` 承接 `/api/transcribe` 的上传大小限制、multipart/form-data 校验、audio part 字节数提取、ASR provider 未配置/未实现响应，`ai_tools.rs` 回收为 AI 评分与知识库 fallback，边界测试确认 ASR 上传细节不再回流到 `ai_tools.rs`，`ai_tools.rs` 从 198 行降到 104 行。 |
| Node auth routes 拆分 | `node tests/node_auth_routes_boundary_test.js`、`node tests/node_auth_boundary_test.js`、`node tests/auth_permissions_test.js`、`node tests/api_runtime_test.js`、`npm test` | 通过；`auth_routes.js` 承接 `/api/auth/session`、`/api/auth/login`、`/api/auth/logout` 兼容 handler 和登录限流，边界测试覆盖错误凭据、成功登录、Secure cookie、session 读取、logout 清 cookie 和 405 分支，`apps/server/app.js` 从 461 行降到 314 行。 |
| Node AI routes 拆分 | `node tests/node_ai_routes_boundary_test.js`、`node tests/node_ai_tools_boundary_test.js`、`node tests/api_transcribe_test.js`、`node tests/auth_permissions_test.js`、`npm test` | 通过；`ai_routes.js` 承接 `/api/score-answer`、`/api/generate-kb`、`/api/transcribe` 兼容 handler、权限校验、请求解析和上传错误处理，边界测试覆盖无权限拒绝、评分 fallback、知识库 fallback、multipart 校验、ASR 未配置响应和音频过大分支，`apps/server/app.js` 从 314 行降到 185 行。 |
| Node runtime store 拆分 | `node tests/node_runtime_store_boundary_test.js`、`node tests/api_runtime_test.js`、`node tests/auth_permissions_test.js`、`node tests/react_functional_persistence_test.js`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional` | 通过；`runtime_store.js` 承接 runtime JSON 路径、envelope、用户 `dataScope`、读写和记录规范化，`apps/server/app.js` 从 1303 行降到 1193 行，Node runtime API、Web 持久化、Rust SQLite 入库和 Android WebView 本地保存读回仍通过。 |
| Node runtime routes 拆分 | `node tests/node_runtime_routes_boundary_test.js`、`node tests/node_runtime_store_boundary_test.js`、`node tests/api_runtime_test.js`、`node tests/auth_permissions_test.js`、`npm test` | 通过；`runtime_routes.js` 承接 `/api/runtime`、`/api/progress`、`/api/reviews`、`/api/applications` 和 `/api/interview-mistakes` 兼容 handler，边界测试覆盖写入、只读拒绝、投递更新和错题删除，`apps/server/app.js` 从 649 行降到 461 行。 |
| Node static files 拆分 | `node tests/node_static_files_boundary_test.js`、`node tests/page_smoke_test.js`、`node tests/mobile_schedule_detail_test.js`、`node tests/kb_mobile_list_test.js`、`node tests/api_runtime_test.js`、`node tests/auth_permissions_test.js`、`node tests/react_functional_persistence_test.js`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional` | 通过；`static_files.js` 承接静态路径、public-safe fallback、content-type、私有静态资源和文件响应，`apps/server/app.js` 从 1193 行降到 1044 行，页面 smoke、移动详情、知识库列表、权限、Web 持久化、Rust SQLite 入库和 Android WebView 本地保存读回仍通过。 |
| Node AI tools 拆分 | `node tests/node_ai_tools_boundary_test.js`、`node tests/api_runtime_test.js`、`node tests/api_transcribe_test.js`、`node tests/page_smoke_test.js`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional` | 通过；`ai_tools.js` 承接 AI 评分 fallback、知识库生成 fallback、provider timeout、JSON 抽取和 AI 结果规范化，`apps/server/app.js` 从 1044 行降到 761 行，Node API、Web 持久化、Rust SQLite 入库和 Android WebView 本地保存读回仍通过。 |
| Node HTTP utils 拆分 | `node tests/node_http_utils_boundary_test.js`、`node tests/api_runtime_test.js`、`node tests/auth_permissions_test.js`、`node tests/api_transcribe_test.js`、`node tests/page_smoke_test.js`、`npm test`、`npm run test:functional`、`npm run test:rust:functional`、`npm run test:android:functional` | 通过；`http_utils.js` 承接安全响应 header、JSON/redirect 响应、请求体读取和 multipart 音频解析，`apps/server/app.js` 从 761 行降到 649 行，Node runtime、认证权限、录音上传闭环、页面 smoke、Web 持久化、Rust SQLite 入库和 Android WebView 本地保存读回仍通过。 |
| 服务器远端验收 evidence 门禁 | `node --check tools/write_remote_acceptance_evidence.js`、`node --check tools/write_remote_invitation_evidence.js`、`node --check tools/write_remote_invitation_account_evidence.js`、`node --check tools/write_remote_login_switch_evidence.js`、`node --check tools/validate_final_delivery_readiness.js`、`node tests/final_delivery_readiness_test.js`、`npm run write:remote-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/acceptance.json`、`npm run write:remote-invitation-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/coach-invitations.json`、`npm run write:remote-invitation-account-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/coach-invitation-account.json --allow-create-account`、`npm run write:remote-login-switch-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/login-switch.json --allow-create-account` | `PASS`；脚本从仓库外私有 `--delivery-env-file` 读取远端 URL 和认证账号，已证明远端 Web 登录、session、health、`/api/progress remote save` 和 `remoteAcceptance` marker 读回；邀请台账 smoke 已证明远端 React 入口、React JS marker、`templateVersion=jd-focus-v1`、批量导入、批量状态更新和 `/api/coach/invitations` 新增/读回/删除；远端邀请账号生命周期 smoke 已证明 `accountProvisioning.enabled=true`、`codex-smoke-account` 可登录、session 读回 username/dataScope、smoke runtime 不串 owner、单账号禁用后登录 401、恢复后登录 200、删除后登录 401，批量禁用两个账号后登录均 401、批量恢复后登录成功、批量删除后被删账号登录 401，并验证 `invitation_notifications_generated=2` 可生成邀请通知草稿，且响应未泄露生成密码或 hash；远端 UI 登录切换 smoke 已证明真实登录页 owner 登录、点击退出、切换 `codex-ui-switch-smoke` 登录、session/dataScope 读回和临时账号清理。 |
| 服务器同步 evidence 门禁 | `node --check tools/write_server_sync_evidence.js`、`node --check tools/restart_remote_service.js`、`node --check tools/validate_final_delivery_readiness.js`、`node tests/final_delivery_readiness_test.js`、`npm run write:server-sync-evidence -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-sync/sync.json`、`npm run restart:remote-service -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/server-remote/service-restart.json` | `PASS`；脚本已验证远端 Linux ELF 与本地 server-delivery manifest SHA-256 一致，并保护 `/shared/***` 运行时数据；重启脚本证明 `job-sprint.service` 已加载当前 manifest，避免旧进程继续响应。 |
| 交付外部输入预检 | `node --check tools/delivery_env_file.js`、`node --check tools/write_delivery_env_template.js`、`node --check tools/init_android_release_signing.js`、`node --check tools/validate_delivery_external_inputs.js`、`node tests/delivery_external_inputs_test.js`、`node tests/delivery_env_template_test.js`、`node tests/android_release_signing_init_test.js`、`node tests/delivery_env_file_entrypoints_test.js`、`npm run init:delivery-env -- --output /path/outside-repo/job-sprint-delivery.env`、`npm run init:delivery-env -- --merge-missing`、`npm run init:android-release-signing -- --delivery-env-file /path/outside-repo/job-sprint-delivery.env --write-env`、`npm run validate:delivery-inputs -- --delivery-env-file /path/outside-repo/job-sprint-delivery.env --allow-user-action` | `USER_ACTION_REQUIRED`；预检可独立暴露服务器 SSH、可选部署 SSH key/port、远端 URL/账号、Android 远端 HTTPS/adb、正式 release keystore/证书 SHA-256 和最终报告路径缺口；已支持仓库外私有 `--delivery-env-file`，并拒绝仓库内 env 文件；`npm run init:delivery-env` 可创建仓库外 `0600` 权限空白模板，只写变量名、可选部署 SSH key/port 槽位和默认最终报告路径，不写真实秘密；已有私有 env 时 `--merge-missing` 只追加缺失变量，不覆盖已有正式签名材料、账号或密码；`npm run init:android-release-signing` 可在无既有长期 release keystore 时生成仓库外私有 keystore，读取证书 SHA-256，并把签名变量写入仓库外私有 env，报告不输出密码明文；服务器同步、远端验收、Android 远端真机和正式 APK 分步入口也支持同一份 `--delivery-env-file`；不会把输入已配置误写成服务器同步、真机验收或正式 APK 已完成。 |
| 最终交付编排入口 | `node --check tools/run_final_delivery.js`、`node --check tools/validate_final_delivery_readiness.js`、`node --check tests/final_delivery_runner_test.js`、`node --check tests/final_delivery_readiness_test.js`、`node tests/final_delivery_runner_test.js`、`node tests/final_delivery_readiness_test.js`、`npm run final:delivery -- --dry-run --delivery-env-file /path/outside-repo/job-sprint-delivery.env --report /tmp/job-sprint-final-delivery-dry-run.json` | 通过；`npm run final:delivery` 会先做工具/env preflight，并在 preflight 中嵌入与 `npm run validate:delivery-inputs` 同源的 `externalInputs` 结构化缺口，再串联 release gate、服务器同步 evidence、服务器远端验收、Android 远端验收、正式 APK 构建和最终 readiness；已支持仓库外私有 `--delivery-env-file`，报告只显示 env 文件加载状态和变量名，不输出秘密值；`--dry-run` 只预览步骤、基础工具/条件工具和缺少输入，不触发 rsync、真机或签名构建；`--report` 或 `JOB_SPRINT_FINAL_DELIVERY_REPORT` 可落盘统一报告；runner 内部最终 readiness 会设置 `JOB_SPRINT_FINAL_DELIVERY_IN_PROGRESS=1` 并传入 `--defer-final-delivery-report`，避免统一报告尚未写入时形成自引用；报告初步 PASS 且写入后会追加 `post_final_report_validation`，post-validation 期间只允许自身步骤尚未写入；最终落盘报告必须包含 `post_final_report_validation=PASS`，旧 7 步报告会被 `npm run validate:delivery` 拒绝；`tests/final_delivery_runner_test.js` 已用 fake `npm` 和仓库外临时 env 文件模拟全 PASS 路径，证明报告路径存在时最终报告可产出 `PASS`，并且会先走延后自报告校验，再走 post-validation；`npm run validate:delivery` 会在未提供报告时标 `final_delivery_report` 为 `PASS_WITH_LIMITS`，提供报告时要求报告非 dry-run、步骤齐全且全 PASS。真实运行仍依赖服务器 SSH、远端账号和 Android 远端真机 evidence。 |
| 最终交付 readiness 门禁 | `node tests/final_delivery_readiness_test.js`、`node tests/final_delivery_readiness_android_remote_test.js`、`node tests/android_remote_functional_entrypoint_test.js`、`npm run validate:delivery -- --allow-dirty` | `PASS_WITH_LIMITS`；server delivery、server sync、server remote、Android remote、formal APK 和 final report 均通过；限制为 dirty worktree 与 P8 理论性架构门禁。 |
| Android formal release gate 探针 | `node --check tools/init_android_release_signing.js`、`node --check tools/build_android_release_apk.js`、`node --check tools/validate_final_delivery_readiness.js`、`node tests/android_release_signing_init_test.js`、`node tests/final_delivery_readiness_test.js`、`npm run init:android-release-signing -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --write-env`、`npm run build:android:release -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/android-release/formal-release.json`、`npm run validate:delivery -- --allow-dirty` | `PASS_WITH_LIMITS`；已生成仓库外私有 keystore、alias、密码和证书 SHA-256，构建工具和 readiness 会拒绝仓库内 keystore；`docs/evidence/android-release/formal-release.json` 为 `FORMAL_SIGNED`，readiness 已重新计算 APK SHA-256 并验证证书 SHA-256 匹配。该项通过不代表服务器同步、远端验收或 Android 远端 evidence 已完成。 |
| 目标验收门禁 | `node --check tools/validate_goal_acceptance.js`、`node --check tools/delivery_readiness_goal.js`、`node tests/goal_acceptance_test.js`、`npm run validate:goal-acceptance -- --allow-user-action` | `PASS_WITH_LIMITS`；HTTPS、服务器同步、正式 APK 与 Android remote evidence 已齐备，P8 目标保留理论性限制。 |
| React 类型与单测 | `npm run typecheck --prefix apps/react-web`、`npm test --prefix apps/react-web` | 通过。 |
| Android debug 构建安装 | `gradle -p apps/android :app:assembleDebug`、`adb install -r apps/android/app/build/outputs/apk/debug/app-debug.apk` | 通过。 |

## 剩余动作

1. 当前 `~/.job-sprint/job-sprint-delivery.env` 已有服务器、账号和正式签名材料；server-delivery、服务器同步、服务器 HTTP 远端 evidence 和正式 APK 已通过。剩余关键动作是修复公网 HTTPS 443 外部握手，让 `https://job-sprint.example.com/job-sprint/react/index.html` 可从外部正常 TLS 访问；随后把 `JOB_SPRINT_ANDROID_WEBVIEW_URL` 改为该 HTTPS URL，执行 `npm run test:android:remote:functional -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env` 生成包含 `authEvidence` 和 authenticated session 状态的 Android 远端 evidence，把报告路径写入 `JOB_SPRINT_ANDROID_REMOTE_ACCEPTANCE_EVIDENCE` 或通过 `--android-remote-evidence <report.json>` 传给 `npm run validate:delivery`；最后再运行 `npm run final:delivery -- --delivery-env-file ~/.job-sprint/job-sprint-delivery.env --report docs/evidence/final-delivery/final-delivery.json` 产出 PASS 统一报告。
2. 正式 APK 已完成本地签名和验签；后续必须保管 `~/.job-sprint/android-release/job-sprint-release.p12` 与 `~/.job-sprint/job-sprint-delivery.env`，不要旋转 keystore。服务器和 Android 远端验收完成后，再安装 `apps/android/app/build/outputs/apk/release/app-release.apk` 并跑 Android 功能验收。
3. 若继续追求 P8 级结构质量，下一刀优先推进远端 session/token 产品化，每次只拆一个语义边界，并保持 `npm run test:functional`、`npm run test:rust:functional`；涉及真机行为时再补 `npm run test:android:functional`。
